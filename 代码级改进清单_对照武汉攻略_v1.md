# 代码级改进清单 v2（对照外部"武汉三天两夜"攻略 + 最新大同成品）

> **v2 修订说明（最高优先级，覆盖 v1 中冲突表述）**：按最新产品定位重写责任边界——
> **项目只做"怎么玩"的真实路线与景点/美食口碑，来源是抖音近期热门攻略视频 + 真实游客评论，主打真实与实时；天气、交通费用、车次航班、实时票价等事务性易过期信息不由项目生成，用户拿名字自查。**
> 因此：v1 的 A2（接天气 API/逐日天气/穿衣）**整体取消**；A1（城际交通信息层）**降级为可选 P2 且不产出任何数字**；新增 **F 主线"真实与实时性"为 P0 之首**；C1 由"学生票报价"改为"平替玩法经验"。
>
> 阅读对象：负责落地的执行模型。本文只做设计、不含实现代码；锚点来自 2026-09-11 完整通读，行号可能漂移，以"文件 + 函数/字段名"为准。
> 架构宪法：决策层唯一产物 `TripPlan`，生成与呈现分离；缺数据标"待核实"，**禁止 LLM 编造**；每条玩法结论可溯源到 official / ugc / baseline。

---

## 0. 产品责任边界（最高准则，一切需求先过这张表）

### 0.1 项目负责什么（UGC 真实 + 实时，是壁垒）
1. **去哪玩 + 路线怎么串**：景点/美食选择、不走回头路的玩法动线、每天怎么排。
2. **怎么玩**：亮点、打卡出片机位、体验方式、最佳时段。
3. **避坑**：游客陷阱、别买什么（速通/加价/捆绑）、排队与预约注意。
4. **真实口碑**：好评/差评倾向、真实评论原话（去标识）、热度与趋势（火/降温）、新鲜度。
5. **能否去成（路线可行性，非事务信息）**：周几闭馆、演出哪几天有、是否需预约——这些决定路线能不能排，必须做。
6. **住哪片**：片区玩法经验与正反建议（不涉及酒店价格/预订）。
7. **景点/美食介绍卡**：标准名/别名/片区 + 真实口碑，**方便用户复制名字去外部搜索查询**。

### 0.2 用户自查什么（项目只给一句"拿名字去查"的提示，绝不生成数字）
| 事务 | 自查渠道 | 项目的处理 |
|---|---|---|
| 天气 / 温度 / 穿衣 | 天气 App | **不接天气 API、不算穿衣**，仅在出发清单放一条无数据提示 |
| 车次 / 航班 / 城际票价 | 12306 / 航司 | 不生成车次号、时长数字、票价；仅 P2 可选给"哪站离哪片近"的 UGC 经验 |
| 市内交通费用（打车/公交多少钱） | 地图 App | `legs` 只给"方式 + 约需多久"，**不含费用（现状已正确，保持，禁止加钱）** |
| 门票实时价 / 购票 | 官方票务 | 项目只给"参考价（带来源与时效）+ 预约提示 + 别买加价速通"，实时价与购买用户自查 |
| 酒店价格 / 预订 | 订房 App | 只建议住哪片，不报价不代订 |

### 0.3 一句话定位（对外话术与取舍依据）
> 一个基于抖音**近期**热门攻略与**真实去过的人**的评论、产出"怎么玩才对"的路线引擎；交付**真实、新鲜、可溯源、可搜索**的玩法路线和景点/美食口碑卡，不包办天气、票价、车次这些用户随手可查且极易过期的事务信息。
> 与通用大模型的根本差异：**它给静态常识，你给"近 90 天真实游客仍在说的话"，并把来源、原话、数据日期摆出来。**

---

## 1. 已实现基线（禁止重复建设）

| 能力 | 位置 | 现状 |
|---|---|---|
| 统一证据对象与三态决策、证据强度 | `pipeline/decision.py` | 已实现 |
| TripPlan / RankItem / PoiDetail / FoodDetail / IntroSection + catalog 双榜下钻 + 生成呈现分离 | `pipeline/decision.py` | 已实现 |
| 质量门禁 R1–R12、最多两轮回炉、金额总账已退役 | `pipeline/qc.py`、`pipeline/planner.py` | 已实现 |
| 官方事实三层降级（种子 yaml > 高德营业时间 > 空） | `core/official_facts.py`、`core/geo.py` | 已实现 |
| 采集质量闸、营销砍半、搜索预筛、详情校验、候补、自动降档 | `core/quality.py`、`crawler/tabs.py` | 已实现 |
| UGC 源默认关闭/opt-in 告知/频控为 0 拒绝/验证码只停不绕 | `crawler/base.py`、`config.py` | 已实现 |
| 多 Tab 并发共享全局令牌桶、拦截重资源 | `crawler/tabs.py`、`core/rate_limiter.py` | 已实现 |
| 评论监听接口 JSON 优先/DOM 降级、`collect_comments=False` 元数据模式 | `crawler/douyin.py` | 已实现 |
| 热度刷榜"首条采评论、其余元数据"两档雏形（最佳范例） | `service/heatrefresh.py` | 已实现 |
| 城市攻略层（热门攻略提炼 itinerary_draft/stay_advice/guide_candidates） | `pipeline/candidates.py`、`planner.py`、`decision.apply_guide_evidence` | 已实现 |
| 餐饮解耦 R7、美食多维评分 | `planner.py`、`decision.py` | 已实现 |
| 防串档、门票唯一口径 pick_ticket_price、catalog 同源回填 R12 | `decision.py`、`planner.py` | 已实现 |
| **市内 legs 只给方式/时长/nature、不含费用（正合新边界）** | `planner.build_legs`、`core/geo.py` | 已实现，保持 |
| start_date 贯通、day_weekdays、R3 文本闭馆匹配 | `api_server/mcp_server/planner/qc` | 部分（见 A3） |
| 热度三因子含新鲜度权重、is_stale 过期降权、情感趋势 | `pipeline/heat.py`、`pipeline/extract.py` | 已实现（报告未显性化，见 F1） |
| 前端三 Tab + 详情弹层下钻 + 只读追问 | `web/index.html`、`templates/trip_report.html` | 已实现 |
| 去标识白名单、keyring、成本闸、token 账本、ASR 即转即删 | `core/*` | 已实现 |
| 3455 行离线测试 | `tests_offline.py` | 已实现，保持并外显 |

---

## 2. 缺口总览与优先级（已按新边界重排）

### P0 —— 直接决定"真实/实时"人设与报告卖相
| 编号 | 缺口 | 一句话 | 主锚点 |
|---|---|---|---|
| **F1** | 证据新鲜度卡（实时性显性化） | 报告头部一眼看到"基于 N 视频 M 评论、近90天占比、数据截至、热度升降" | `decision.HeatInfo`、TripPlan.meta、`trip_render`、模板 |
| **F2** | 新鲜度链路 + 一键刷新接线 | 过期降权已具备，缺"出发前刷新"入口与时效提示 | `heat.py`、`service/heatrefresh.py`、`knowledge.py`、web |
| **F3** | 真实口碑证据链前置到详情卡 | 真实评论原话/好评差评/独立来源数/营销预警从附录提到详情主体 | `decision.build_poi_detail/build_food_detail`、`pipeline/report.py`、模板 modal |
| **F4** | "可搜索清单"化 | 标准名/别名/片区 + 复制去搜 + "其余信息自查"提示，交付物=决策+搜索清单 | PoiDetail/FoodDetail、`web/index.html` |
| A3 | 结构化闭馆 + **演出场次**校验 | 现只拦"景点周一闭馆"文本，拦不了知音号休演/保元风云仅周三~周日 | `decision.OfficialFact`、`qc` R3、种子 yaml |
| A4 | 逐日显示日期/星期 + 挪点显性化 | 有 start_date 却只写"第N天"，用户看不到你替他避开周一 | `trip_render`、itinerary、模板 |
| B1 | 第三方加价/速通从 notes 漏网 | 门票卡待核实，notes 却写"携程899速通/导游50/拼车40" | `planner` notes 归一、`qc` R12 扩文本 |
| B2 | Plan B 名不副实 + 同句重复5遍 | 应是雨天/排队/体力应急预案，现在堆备选理由且重复 | `decision.build_trip_plan`、`trip_render` |
| B3 | 热度榜后半全 0.00 噪声 | 未调研点不进榜（决策表仍保留） | `decision.to_rank_items/to_food_rank_items` |
| B4 | 简介半截话/美食串景点 | "善化寺拥有"残句、"沙棘冰面包"当景点简介 | `decision.build_poi_detail`、抽取 prompt |
| B5 | 动线 warn 不修、远郊时间漏计 | 绕行33km只提示；悬空寺单程95min 没进 R4 | `qc` R4/R5、`planner` 通勤解析 |

### P1 —— 玩法丰富度、提速、开源转化
| 编号 | 缺口 | 一句话 | 主锚点 |
|---|---|---|---|
| C1 | 平替玩法/性价比体验（**非报价**） | 免费平替机位、1.5元轮渡这类"怎么玩更值"的 UGC 经验；不做学生票结构化报价 | PoiDetail 增 free_alternative/play_tip、抽取 |
| C2 | 体验标签闭集 + 体验类候选配额 | 出片/沉浸/夜生活/citywalk/演出；别让体验点在截断里全灭 | PoiDetail/RankItem.tags、候选配额、前端筛选 |
| C3 | 美食 meal_scene/tourist_trap/品类vs门店 | 过早/正餐/宵夜、户部巷是坑+本地替代、品类与门店分组 | `decision.FoodDetail`、美食抽取、美食榜 |
| C4 | 片区/主题动线 + 住哪片结构化 | "汉口烟火→武昌人文→东湖潮玩、住哪避哪" | 攻略层 areas、planner 聚簇、TripPlan |
| D1 | 两档采集主流程组装 + 密度切 comment_count | 零件齐备，主流程仍每条抓评论；密度没用平台总评论数 | `heat.py:heat_index`、`tabs.py`、`service/trip._crawl` |
| E2 | 零门槛 demo / Pages / 双语 README | clone 后要配Key+登录才能看效果=最大 Star 流失 | 内置脱敏样例 + `--demo` + Pages |
| E4 | 改参数一键重排闭环 | 前端不回填参数，ask 只读不能重排 | `web/index.html:goTrip` |

### P2 —— 可选打磨
| 编号 | 缺口 | 说明 |
|---|---|---|
| A1' | **可选** UGC 经验型"抵达片区提示" | 仅当 UGC 真实提到时给一句"多数人到 XX 站，离 XX 片区近"，**不给车次/时长/费用数字**，附"车次票价自查12306"；不做不影响主线 |
| W0 | 天气纯自查项 | 只在 `pipeline/checklist.py` 加一条"出发前自查天气 App"，**不接天气 API、不做温度穿衣**；确定性星期/节假日日历保留（服务 A3/错峰，非天气） |
| E1 | product/PRD.md 与现状脱节 | 仍写预算明细/Leaflet，更新或删除，避免文档打脸代码 |
| E3 | 定位话术升级 | "防幻觉证据驱动 Agent 决策框架（旅游是 demo）" |
| E5 | 新功能补离线测试 + CI 徽章 | 延续测试文化并外显 |

---

## 3. P0 详细设计

### F1 证据新鲜度卡（把"实时性"做成第一卖点）
**现状**：`decision.HeatInfo` 已有 videos/likes/comments/fresh_ratio/sentiment/trend，`heat.py` 已算近 90 天新鲜度与四态趋势，但报告只在角落展示，用户感知不到"这是近期活数据"。
**设计**：
1. TripPlan.meta 增 `evidence_window`：`{video_count, comment_count, content_earliest, content_latest, fresh_90d_ratio, crawled_at, source_city, ttl_days}`，由 service/trip 汇总各点数据现算（不新造数字）。
2. 报告/HTML **最顶部**渲染"证据新鲜度卡"：本路线基于 N 条热门攻略视频、M 条真实评论，内容集中在 X 月–X 月，近 90 天内容占比 X%，数据采集于 X；并给整体热度趋势（升温/稳定/降温）。
3. 每个榜单条目与详情卡显示该点"最新内容时间 / 近90天占比 / 趋势箭头"，让"新不新鲜"逐项可见。
**AC**：无 UGC 时该卡显式说明"未取得近期 UGC，以下为基线结果"而非隐藏或造假；有数据时各数字可由原始 items 复算。
**测试**：evidence_window 纯汇总函数；空数据降级；fresh_ratio 与 items 时间一致。

### F2 新鲜度保障 + 一键刷新
**设计**：①确认 `extract.is_stale` 过期降权与 `heat` 新鲜度因子对老内容正确压权（补测试固化阈值）；②`knowledge` 缓存 TTL 到期自动重取并在报告标"部分热度来自 X 天前缓存，可刷新"；③把 `heatrefresh` 的刷榜能力接到 web 报告页"刷新热度/重新生成"按钮，并在报告注明"攻略有时效，出发前建议刷新"。
**AC**：缓存命中/过期路径可区分；刷新不触发违规频控（走全局令牌桶）。

### F3 真实口碑证据链前置（把"真实"做成可核验）
**现状**：真实评论原话摘要在 `pipeline/report.py` digest、溯源链接在报告附录，详情卡主体反而是结构化空字段。
**设计**：
1. `build_poi_detail/build_food_detail` 增 `comment_samples`（去标识真实原话，带相对时间/赞数，extract 已保证 quote 真实出现）、`verdict_counts`（好评/中评/差评条数或占比）、`independent_sources`（独立视频数）、`mkt_warning`（mkt_ratio≥0.5 时"营销号偏多"）。
2. 详情卡（MD 与 HTML modal）以"真实去过的人怎么说"为主体区块：先口碑倾向与样本量，再 2–4 条原话，再亮点/避坑，最后溯源链接；营销内容显式打标，不与真实口碑混。
3. 保持去标识白名单（sanitize），原话不得出现昵称/UID/属地。
**AC**：每条原话都能在原始 items 找到；营销占比高必出预警；无评论时显示"暂无足够真实评论"而非编造。

### F4 交付物"可搜索清单"化
**设计**：PoiDetail/FoodDetail 增 `aliases`（别名/俗称）与 `area`（片区）；详情卡与榜单条目突出"标准名 + 别名 + 片区"，web 弹层加"复制名称去搜"按钮，并固定一行小字"门票实时价 / 营业时间 / 天气 / 交通费用请复制名称到对应 App 自查"。把交付物明确为"帮你决定去哪、为什么、避什么坑的搜索清单"，而非包办说明书。
**AC**：复制内容只含名称/别名，不含任何编造的票价天气。

### A3 结构化闭馆 + 演出场次（武汉周一陷阱 Case）
**现状**：`OfficialFact.close_day` 是自由文本，R3 靠子串匹配且只管景点；拦不了"知音号周一休演、保元风云仅周三–周日 19:30"。
**设计**：
1. OfficialFact 增 `closed_weekdays:[0-6]`、`show_schedule:{weekdays:[],sessions:[],note}`、`blackout_dates:[]`，同步 `normalize_official` 与种子 yaml（北京.yaml 做示范）。
2. R3 结构化优先、文本兜底；校验对象扩到 体验/演出类；命中时给"本趟可挪到周几/几号"的可执行建议供 planner 回炉。
**AC（Case D 进测试）**：武汉 2026-09-21 周一、3 天，省博/知音号/保元风云不得排周一并被挪到周二三；纯文本 close_day 旧路径不回归。

### A4 逐日日期/星期 + 挪点显性化
itinerary 每天带 date/weekday（有 start_date 时），标题渲染"第1天 · 9月21日 周一"；当天有被 A3 挪走的点时显示"今日周一：XX 闭馆/休演，已安排到周三"。无 start_date 优雅退回"第N天"。

### B1 文本层堵第三方加价
在 planner notes/tips 归一与 qc（R12 扩展或新增 R13）扫描自由文本：命中"速通/优速通/代抢/代预约/捆绑套餐/含票套餐/找我订/加微信/携程买票"等购买建议一律剔除并记 removed_items（只允许以避坑口吻出现"别买 XX 速通"）；区分官方门票 / 必要现场消费（进山大巴、官方讲解，标 necessary 且不与门票相加）/ 第三方加价（禁）。**注意：交通费用本就不该出现，notes 里"拼车40元"这类交通报价一并按边界清除。**

### B2 Plan B 正名 + 去重
固定文案（尤其"餐饮解耦"）只保留一次；备选理由挪回决策表。TripPlan 增 `contingency` 三槽：weather（雨天/高温室内替代，只用已入选室内点）、queue_or_soldout（排队/约满替代）、lite_version（体力精简版，砍哪个点+砍后动线）；planner 给每点标 indoor/outdoor 支撑 weather 预案。缺料标"暂无足够证据"不硬编。

### B3 双榜去 0 分噪声
`to_rank_items/to_food_rank_items` 只收 heat.videos>0 或有 guide_heat 补位的点；未调研/0 分点不进榜（决策表仍全量保留，不违反 R2）；默认 Top N（建议各 10，可配），不足时用现有 food_sample_note 同款机制说明。

### B4 简介质量纯函数
新增 summary 校验：长度下限、残句检测（"的/了/拥有/是"结尾无句号判截断）、不得取自 avoid、景点简介不得含美食话术；不合格置空标"简介待补充"，绝不显示残句。抽取 prompt 固定中性概述句式"XX 是一座/一处…，以…著称"，概述/亮点/避坑取数优先级写死，禁止互相兜底。

### B5 通勤口径统一 + 远郊聚簇 + 动线可修
把 block.transport/notes 里的时长解析成分钟并入 R4（标 nature=估算/实测）；单程>60min 的远郊点必须聚簇、不得与市区点横跳，否则提示"建议单独半天/一天"。R5 从只 warn 升级为"warn+建议顺序"，二轮回炉真正重排。与 C4 共用地理聚簇能力。

---

## 4. P1 设计要点

- **C1 平替玩法（非报价）**：保留 `free_alternative:{name,how}`（如司门口红墙免费拍黄鹤楼）、`play_tip`（如"1.5 元轮渡是性价比体验项目"，属玩法不是通勤报价）；**删除学生票/票价类结构化字段**，UGC 原话若提学生优惠只作评论/tip 展示，不作为价格。来源走攻略层/UGC 抽取，带来源。
- **C2 体验标签**：vibe_tags 闭集（出片机位/沉浸式演出/夜景夜生活/citywalk/博物馆室内/自然户外/亲子/脱口秀）；候选截断给体验类保留最低名额（治 B3 体验点全灭）；前端可按标签筛。
- **C3 美食**：FoodDetail 增 food_kind（品类/门店/街区）、meal_scene（过早/正餐/宵夜/加餐）、tourist_trap{is_trap,why}、local_alternative；品类与门店分组不混排；陷阱置底并进避坑区，给本地替代。
- **C4 片区住宿**：攻略层增 areas[{name,theme,pois,stay_pro,stay_con,recommend}]，planner 结合坐标聚簇排天，TripPlan 增"片区与住宿"模块（只讲住哪片/避哪片坑，不报价）。
- **D1 两档采集（不要全局 comments=0）**：①`heat.heat_index` 密度从采样评论数改为平台 `item.comment_count`（不抓评论也能算热度，采样评论只供情感/避坑）；②阶段A 全量 `collect_comments=False` 元数据快扫→基线/攻略收敛入围→阶段B 仅入围点精抓评论（抄 heatrefresh 的 per_item_kwargs）；③heat-only/balanced(默认)/deep 三档，在 service/trip._crawl 接线、config 增开关。
- **E2 开源转化**：内置脱敏样例 + 一份跑好的 TripPlan JSON + `--demo`（不联网/不登录/不调 LLM 直接渲染）；GitHub Pages 放北京+武汉 HTML；首屏放 GIF+Pages+"通用大模型 vs 本引擎（武汉周一陷阱）"对照；双语 README。
- **E4 一键重排**：结果区回填 city/days/style/start_date + 快捷操作（+1天/改省钱/换日期/去掉某点）；ask 只读、重排走 /api/trip，形成 Plan-Check-Repair 闭环。

---

## 5. P2 与明确不做

- **A1' 抵达片区提示（可选）**：仅 UGC 真实提到时给"到哪个站离哪片近"的经验一句，**禁止车次号/时长/票价**，固定附"车次票价以 12306 为准"；市内 legs 维持只给方式与约需时长、不加费用。
- **W0 天气**：不接任何天气 API、不生成温度/穿衣/逐日天气；仅 checklist 一条自查提示。确定性"星期/法定节假日日历"保留（标准库 datetime + data/holidays.yaml，服务闭馆演出与错峰，与天气无关）。
- **E1/E3/E5**：同步 product/PRD.md（删预算/Leaflet 过时表述）、定位话术升级为"证据驱动 Agent 决策框架（旅游 demo）"、新功能补离线测试并挂 CI 徽章。

---

## 6. 黄金回归用例（进 tests_offline.py）

- **Case D 周一陷阱（A3+A4）**：武汉 2026-09-21 起 3 天，省博/知音号/保元风云不排周一、被挪周二三，标题显示日期星期与挪点说明。
- **Case R 实时性（F1/F3）**：给定带时间戳的 items，evidence_window 与 fresh_ratio 可复算；详情卡 comment_samples 均能在原始数据找到，营销高占比必预警；无 UGC 时显式标基线、不造假。
- **Case E 边界（B1+0.2）**：slot 文本含"携程速通/拼车40元/打车多少钱"必被清除且不产生任何交通费用数字；legs 序列化结果不含 price/amount/费用字段。
- **Case W 天气不生成（0.2/W0）**：TripPlan 与渲染结果中不出现 temperature/天气数值/穿衣结论字段，只有一条自查提示。
- **Case F 远郊聚簇（B5）**、**Case G 榜单无噪声（B3）**、**Case H 两档采集（D1）**：同 v1。
- 既有 Case A 北京 / B 大同 / C 一致性保持全绿。

---

## 7. 红线（执行模型必读）

1. **不生成天气/温度/穿衣、不生成车次航班、不生成任何交通费用数字、不做票务代订**；这些只给"复制名称自查"提示。
2. **市内交通永远不含费用**（legs 只给方式+约需时长）；门票只给带来源的参考价/预约提示/防加价，实时价用户自查；**金额总账不得回归**。
3. **不全局 comments=0**：非入围点不抓、入围点精抓，避坑与情感依赖真实评论。
4. 不让 LLM 编任何数字（票价/时刻/温度/距离），缺证据标"待核实"是招牌不是缺点。
5. 不做地图瓦片/轨迹、代理 IP 池、签名逆向、过验证码；UGC 源继续默认关闭 opt-in。
6. 新字段先进 TripPlan 数据契约，MD/HTML/API 三端只读同一对象，表达层不二次计算、不调 LLM。

---

## 8. 落地顺序（每步独立可提交、可验收）

1. **第一批（真实实时 + 卖相，纯内核）**：F1 新鲜度卡 → F3 证据链前置 → B3 榜单去噪 → B2 PlanB 去重正名 → B4 简介质量 → B1 文本边界拦截 → A4 日期星期。
2. **第二批（路线可行性）**：A3 闭馆演出 + Case D → F2 刷新接线 → F4 搜索清单化 → B5 远郊/动线。
3. **第三批（玩法丰富度）**：C1 平替玩法 → C2 体验标签 → C3 美食场景/陷阱 → C4 片区住宿。
4. **第四批（采集提速）**：D1 密度切 comment_count → 两档主流程组装 → 三档开关。
5. **第五批（开源门面，并行）**：E1 文档同步 → E2 demo/Pages/双语 → E4 重排 → E3 定位 → E5 CI；P2 的 A1'/W0 随手做。

> 每批完成：补离线测试 → tests_offline.py 全绿 → 用武汉/大同样例回归一份报告，对照第 6 节黄金用例肉眼验收。
