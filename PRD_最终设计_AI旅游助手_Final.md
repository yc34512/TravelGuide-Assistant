# PRD 最终设计：UGC 证据驱动的可解释 AI 旅游助手（开源实现版 · Final）

> **文档性质**：产品需求文档（PRD）+ 实施规格说明（**最终设计 / 产品形态冻结版**）
> **读者**：AI 编码代理 / 接手开发的工程师。本文件自包含，可直接据此开发，无需口头补充。
> **版本**：v3.0 Final ｜ 制定日期：2026-09-05
> **与历史文档关系**：本版**取代并包含** `PRD_行程决策引擎升级_开源版.md`（v2.0）。v2.0 中已实现/已验证的内容在本版标注「已完成·勿重做」，其余为待实现项。
> **产品定位**：一个 UGC 证据驱动、**可解释、可核验**的避坑型 AI 行程决策开源项目。平台中立的分析内核 + 可插拔的采集适配器。
> **最高目标**：GitHub 开源、获取大量 Star、成为求职简历上的硬核个人项目（Agent / RAG / 数据管线方向）。**非商业项目。**

---

## 0. 给 AI 编码代理的协作约定（先读 · 硬约束）

1. **只在现有架构上演进，禁止推倒重来。** 现有分层（`core / crawler / pipeline / service / web / templates / api_server / mcp_server`）、异步任务系统（job/取消/历史落库）、LLM 输出先 normalize 再使用的防御性规范、纯函数 + `tests_offline.py` 离线测试、Markdown+HTML 双输出、三端入口、合规设计（频控/去标识化/凭据管理）都是被验证过的资产，**必须保留并延续其代码风格**。
2. **本版为最终产品形态，不再变更信息架构。** 用户感知到的报告固定为三段：**攻略介绍（What）→ 行程规划（How）→ 热度榜 + 美食榜（Explore，可下钻详情）**。实现只能在该形态内填充，不得自创第四种主模块、不得把餐饮塞回行程时间线。
3. **生成与呈现分离（本版新增的核心硬约束）。** 决策层只产出结构化 `TripPlan` 对象；Markdown / HTML / API 三种呈现都只读这同一个对象渲染。**严禁在模板里临时计算结论、补数字、改决策。**
4. **一次核查、三处同源。** 介绍、行程、榜单详情中出现的同一事实（票价、开放时间、避坑点、人均）必须读同一个证据对象，**禁止三个模块各让 LLM 生成一遍**，否则同一事实会出现多个版本。
5. **逐条实现，每条 Feature 必须同时交付：功能代码 + 离线测试。** 测试不依赖网络、真实 LLM、浏览器（一律夹具驱动）。
6. **缺数据的默认决策**：结构不合法 → normalize 为安全默认，绝不抛裸异常中断主流程；数据缺失 → 显式标「待核实/未知」，**禁止臆造数字、禁止静默丢弃**；规则冲突 → 保正确性下限 > 保体验完整度。
7. **合规红线不可协商（见 §7.7）**：不实现绕过验证码/登录墙/签名/设备指纹的能力，不内置账号池/代理池/他人凭证；提速不得突破全局限速节奏。
8. **不引入未经本 PRD 批准的重型依赖**；技术栈固定 Python 3.11、DrissionPage、FastAPI、Jinja2、SQLite、OpenAI 兼容 LLM；重依赖（如 faster-whisper）必须改为可选分组。
9. **迁移走双写过渡（见 §5.3）**，每切一个模块保持既有离线测试全绿；全部切完再单独一次提交删除旧拼装代码。
10. 每完成一个里程碑跑通 §15 黄金回归用例并保证既有离线测试全绿。自然语言/条件式描述的规则，实现方式自定，但**对外可观测行为必须与 AC 一致**。

---

## 1. 产品定位、差异化与现状问题

### 1.1 一句话定位（仓库 About / 简历标题）

> 一个基于短视频真实评论的**可解释 AI 行程决策引擎**：自动完成 UGC 采集 → 观点抽取 → 置信度交叉验证 / 营销号识别 → 官方事实校准 → 带质量门禁的闭环规划，最终产出「介绍 + 行程 + 可下钻榜单」三段式报告；**每个推荐可溯源、每个落选有依据、每个数字标性质**。

### 1.2 与两类对手的差异化（必须在产物里看得见）

- **对比通用大模型一次性写攻略**：对方是「一次成稿、无自检、数字靠记忆」；本项目是「真实 UGC 证据 + 官方事实 + 独立质量门禁回炉 + 可展示的选点决策表」。
- **对比 OTA（携程/马蜂窝榜单）**：对方给的是「消费后评分与营销榜单」；本项目给的是「临场避坑情报、最新踩雷、本地视角、争议点预警」，且不做交易、只做个人决策辅助。

### 1.3 已诊断问题总表（本 PRD 系统性解决；状态以实际代码为准）

| 编号 | 现象 | 本质 | 状态 |
|---|---|---|---|
| P-1 | 调研 8 点只排 4，热度第一的主题乐园被静默丢弃 | 规划开环、无独立质检回炉 | 已有最小版 `_coverage_issues`，待升级 R1~R12 |
| P-2 | 验证产出的证据强度/风险/淘汰理由中途被压成布尔值 | 证据未作为统一对象贯通 | 待修（Epic A） |
| P-3 | 漏算主题乐园门票却写「主要花在门票」；大同被 7 元小吃拉低到 255 元 | 预算口径错、缺官方校准 | 部分修复，待官方层与刚性/弹性拆分 |
| P-4 | 两晚全空；「最佳=晚上」的点排下午；不知周一闭馆 | 无时间可行性硬约束 | 提示词已约束，待确定性校验 |
| P-5 | 9 月行程仍写 8 月已结束活动/旧价 | 事实无有效期 | 待修（Epic D 时效） |
| P-6 | 12 条避坑 9 条属未入选点；档案串别景点贴士 | 缺主体归属校验 | 待修（F-A3） |
| P-7 | 只调研 1 家餐厅，正餐槽空、渲染「美食:无」却照算餐费 | 内容缺失与费用矛盾 | **本版改为「餐饮解耦 + 美食独立成榜」根治** |
| P-8 | 热度在规划之后才算，仅展示不参与决策 | 数据生产顺序错位 | 待修（证据前置） |
| P-9 | DOM 滚动 + 固定 sleep + 单标签串行，耗时长 | 采集低效、与决策耦合 | **F8.1~F8.3 已完成（实测单景点约 300s→66s），勿重做**；余 F8.4/F8.6 |
| P-10 | 开源无截图/无 Demo/无样例，跑通需自备 Key+小号+长等待 | 缺零门槛体验路径 | 待修（Epic H） |
| P-11（新） | 报告是线性大文本，榜单点不开详情，介绍/行程/详情数字各说各话 | 缺结构化数据模型与「生成-呈现分离」 | 本版新增（Epic E/F/G） |
| P-12（新） | 美食数据极薄（北京仅 1 家），无法支撑「美食榜」 | 美食无独立采集与排序口径 | 本版新增（F-F2） |
| P-13（新） | 采集评论最慢且最易触发风控、合规风险最集中 | 全量抓评论、采集与发现未分层 | 本版新增两档采集 + 三档模式（§7.2/7.3） |

### 1.4 已完成基线（实现前必读：避免重做，更要避免改坏）

以下能力在 v2.0 定稿后已提交（规划质量 P0、采集提速、采集回归修复），**在其上演进，不得推倒重写**：

- **规划质量最小闭环**：`planner._coverage_issues`（排点过少 / 晚上型错位 / 地标静默丢弃）+ 带问题清单回炉一次，且只采纳「问题数严格变少」的版本；预算 `tickets_detail / tickets_missing`；美食保底救回与 `_filter_fabricated_food` 防编造。
- **采集提速（已实测）**：评论走监听接口 JSON（DOM 降级）、元数据走详情接口 JSON、拦截重资源、条件等待替代固定 sleep、多标签并发 + **类级共享预约式令牌桶**（`CRAWL_TABS` 默认 3，设 1 回串行）。云冈石窟 5 视频 ×3 标签：约 300s→**66s**，评论 24→**180 条**且 100% 带精确时间戳，文案完整率 5/5。
- **既有工程资产**：离线测试与 CI（ubuntu + Py3.11）、知识库 7 天缓存、API 输入校验与防目录穿越、keyring 存 Key、评论去标识化。

---

## 2. 最终用户旅程与产品形态（顶层设计）

### 2.1 输入契约

用户提供：目的地（城市或景区）、天数、预算、住宿位置、人群/偏好/节奏、指定想去/排除的点、**采集档位（CollectMode，默认 balanced）**。缺省项给安全默认并在报告回显。

### 2.2 三段式产品形态（用户感知顺序，冻结）

1. **攻略介绍（What · 建立认知）**：这地方什么样、最佳季节、怎么抵达与市内交通、整体注意事项、入选点简介。
2. **行程规划（How · 核心决策）**：按天 × 上午/下午/晚上给出 **去哪玩 → 怎么去（交通）→ 门票（价格/预约/放票/闭馆）**，打卡点与避坑点内嵌到对应点位。
   - **产品决策（冻结）**：**可以不排「几点在哪吃」，餐饮不进入行程时间线**；行程只在需要处写「就近用餐，推荐见美食榜 #锚点」。但 **去哪玩、怎么去、门票三项为强制项，缺一即质量门禁失败。**
3. **热度榜 + 美食榜（Explore · 开放探索，可下钻）**：两个可排序榜单；点击任意榜单项 → 展开/跳转到该 POI 或美食的**详情档案**。

### 2.3 用户感知顺序 ≠ 底层生成顺序（关键）

用户看到的是「介绍→行程→榜单」，但底层必须**先建数据底座，再让三个视图同源渲染**：

```
                     ┌─────────────────────────────────────────────┐
   多源输入          │ 基线常识 │ 官方事实(优先) │ 抖音UGC │ 携程UGC(可选) │
                     └─────────────────────────────────────────────┘
                                          │  SourceAdapter 统一接口（§7.1）
                                          ▼
   ① 采集适配层  两档采集（元数据快扫 → 基线收敛 → 入围精抓）/ 缓存 / 增量 / 全局限速
                                          ▼
   ② 数据底座    候选(公平不截断) → 官方事实 → UGC验证(证据全保留) → 热度/情感/营销号
                                          ▼
   ③ 证据层      统一 SpotDecision / FoodDecision（一次核查，多处复用）
                                          ▼
   ④ 决策层      介绍生成 + 行程 Plan → 质量门禁 Check(R1~R12) → 回炉 Repair(≤2) → 预算求解
                                          ▼
   ⑤ 模型聚合    唯一顶层对象 TripPlan（intro/itinerary/rankings/catalog/budget/quality）
                                          ▼
   ⑥ 表达层      Markdown(目录锚点) │ HTML(榜单可点下钻) │ API/MCP(JSON)   ← 只读 TripPlan
```

### 2.4 三端入口（保持）

CLI（`main.py / run_cli`）、Web（FastAPI + `web/index.html` + Jinja2 templates）、MCP（`mcp_server`）共用同一决策内核与 `TripPlan`，差异只在表达层。

---

## 3. 目标、非目标与成功度量

### 3.1 目标（按重要性）

1. **质量立得住**：选点合理、时间可行、预算自洽、避坑准确、时效新鲜、来源可溯，达到可公开展示、经得起逐条挑刺。
2. **形态说得清**：三段式 + 可下钻榜单，访客一眼看懂它和「大模型一次成稿」「OTA 榜单」的区别。
3. **开源跑得通**：30 秒看懂（首屏素材）、3 分钟零配置看成品（Demo）、开发者顺畅二开。
4. **简历讲得出**：沉淀为体现 Agent/RAG/数据管线工程能力的代表作（§17.4）。

### 3.2 非目标（Out of Scope，本期不做）

- 不做任何商业化：无付费墙/会员/SaaS/账号体系/服务器运营。**「换付费数据商/平台合作」仅作为架构演进愿景写入 §18，不排期、不写实现。**
- 不做多账号/代理 IP 池、不以签名逆向/设备伪造作为默认或内置通道；不做验证码自动通过。
- 不做小程序 / 原生 App / 在线托管；不做地图与地理轨迹渲染；不做实时订票/交易/抢票；不做社交。
- 不追求运营化覆盖全国城市，数据由用户按需采集 + 本地知识库沉淀。

### 3.3 成功度量

| 类别 | 指标 | 目标 |
|---|---|---|
| 质量 | 黄金回归（§15）通过率 | 100% |
| 质量 | 离线测试 | 全绿，新增能力覆盖率不低于现状 |
| 一致性 | 介绍/行程/详情同一事实一致率 | 100%（同源，R12 可离线断言） |
| 一致性 | 榜单-详情无死链率 | 100%（每个榜单项都有 catalog 详情，R11） |
| 体验 | Demo 启动到完整报告 | ≤60s、零 Key、零联网采集 |
| 性能 | balanced 档端到端 | 较现状显著下降（§14.1），且不靠放宽频控 |
| 开源 | README 首屏 | 1 动图 + 3 成品图 + 在线样例 |
| 开源 | 跨平台 | Demo/决策/表达三平台保证；采集层分层承诺（§14.5） |

---

## 4. 架构宪法（所有需求服从于此）

1. **证据是一等公民**：每个地点从提名到落稿全程携带同一组证据，任何阶段不得压扁成布尔值或一段自由文本后丢弃结构化字段。
2. **规划必须闭环（Plan→Check→Repair）**：生成与裁判分离，确定性规则做门禁，不达标带具体问题回炉，仍不达标**显式声明妥协**，禁止静默。
3. **多源分层 + 冲突仲裁**：基线保下限、官方保硬正确、UGC 保新鲜体感；仲裁序 **官方事实 > 多源高置信 UGC > 单源 UGC > 模型推测**，并**可见标注分歧**。
4. **可信度可感知**：回答「凭什么去 / 为什么没去 / 哪些要自己再确认」。
5. **采集与决策解耦**：改天数/预算/偏好/住宿时，决策层基于已沉淀档案重算，不重新采集。
6. **生成与呈现分离（新）**：决策层只产 `TripPlan`，表达层只读渲染；同一事实全局同源唯一。
7. **一切 LLM 输出不可信，先 normalize；关键结论可降级、可离线复算、可测试。**
8. **合规是特性不是负担（新）**：平台中立、默认安全采集档；提速不以提高风控/法律风险为代价，节奏由全局限速器兜底。

---

## 5. 目标分层架构与模块映射

### 5.1 分层职责（模块边界，硬约束）

- **采集适配层**只负责取数与去标识化，不做选点/评价；以 `SourceAdapter` 接口屏蔽平台差异。
- **数据底座/证据层**只做结构化判断（候选、官方事实、立场、置信度、热度、营销号），不做排程。
- **决策层**只消费证据层产物，不直接读原始评论、不直接发起采集；产出 `TripPlan`。
- **表达层**只呈现 `TripPlan`，不反向修改结论；发现问题应回传质量门禁，而不是在模板里掩盖。

### 5.2 现状模块 → 目标职责映射（以实际代码为准）

| 现状 | 目标职责 | 改动类型 |
|---|---|---|
| `crawler/douyin.py / tabs.py / browser.py` | 收敛为 `DouyinAdapter`（实现 SourceAdapter），保留已完成提速 | 重构接口、不改提速内核 |
| 新增 `sources/base.py / official.py / ctrip.py` | SourceAdapter 抽象 + 官方/携程适配器 | 新建 |
| `pipeline/candidates.py` | 候选圈定/验证，公平截断、证据全保留 | 改造 |
| `pipeline/extract.py / verify.py / heat.py` | 证据层；热度口径升级（§7.4） | 演进 |
| 新增 `pipeline/decision.py` | SpotDecision/FoodDecision/TripPlan 数据契约 + 组装适配层 | 新建（v2 已规划，继续） |
| `pipeline/planner.py` | 时间窗/动线/预算/门禁/回炉；**移除时间线餐饮硬槽** | 演进 |
| 新增 `pipeline/intro.py` | 攻略介绍（What），只读证据对象 | 新建 |
| 新增 `pipeline/rankings.py`、`pipeline/catalog.py` | 双榜排序 + 详情库组装 | 新建 |
| `pipeline/render.py` + `templates/` | 统一 TripPlan → MD/HTML 双渲染、榜单下钻 | 重构 |
| `service/trip.py / research.py / heatrefresh.py` | 编排：采集→证据→决策→TripPlan→渲染；删除五 dict 平行拼装 | 双写过渡后收敛 |
| `core/` | llm/knowledge/sanitize/rate_limiter/geo/asr，补成本统计与要点缓存 | 演进 |
| `api_server.py / mcp_server.py / web/` | 输出 TripPlan JSON 与下钻视图 | 演进 |

### 5.3 迁移策略（必须遵守，否则违反 §0.1）

现状是 `profiles / spot_points / heat / pitfall / food_profiles` 五个平行 dict 在 `service/trip.py` 拼装。采用**双写过渡**：①新建 `decision.py` 定义契约并从旧五 dict 反向填充，旧签名/返回不动；②门禁与决策表、双榜、详情**只读新对象**，旧渲染继续读旧结构；③逐模块切换（预算→避坑→热度→介绍→榜单→详情→模板），**每切一个保持离线测试全绿**；④全部切完单独一次提交删除旧拼装，便于回退。

---

## 6. 统一数据契约（实现前先对齐，字段级）

> 逻辑字段契约，命名可贴合现有风格，但语义、可空性、关联关系必须满足。所有可展示对象带稳定 `poi_id`。

### 6.1 枚举

- `SourceTier`：`baseline / official / ugc_douyin / ugc_ctrip / model_guess`
- `Confidence`（对外唯一一套）：`强 / 中 / 弱`（映射见 6.2，禁止两套徽章并行）
- `DecisionState`：`selected / alternate / dropped`
- `BudgetNature`：`官方确定 / UGC参考 / 基线估算 / 待核实`
- `BudgetKind`：`rigid（刚性：门票/交通） / flexible（弹性：餐饮/购物）`
- `Slot`：`morning / afternoon / evening`（对外显示上午/下午/晚上）
- `Trend`：`本周最火 / 长盛不衰 / 正在降温 / 平稳 / 近期热度上升`
- `CollectMode`：`heat_only / balanced / deep`
- `FoodType`：`正餐 / 小吃 / 饮品 / 夜市`；`FoodTag`：`本地老字号 / 网红 / 连锁 / 苍蝇馆子` 等

### 6.2 `SpotDecision`（单个地点全生命周期对象）

| 字段组 | 字段 | 说明 |
|---|---|---|
| 标识 | `poi_id / name / category / aliases[]` | 全链路以此归并，**严禁跨主体串档**；aliases 用于同名归并 |
| 来源 | `source_tags[]` | 被哪些源提名/佐证 |
| 候选 | `candidate_reason / category_quota` | 基线圈定理由与配额来源 |
| 验证 | `verify.verdict(keep/drop) / evidence(强/中/弱) / pitfall_risk(低/中/高) / reason / n_sources / conf_level(内部)` | **淘汰点也全保留到最终决策表**；对外只暴露 evidence：强=高置信且 n≥3，中=中置信，弱=低置信/单源/营销 |
| 热度 | `heat.score / score_breakdown{like,density,fresh} / trend / videos / likes / platform_comment_total / fresh_ratio / mkt_ratio / controversy_ratio / sentiment` | 规划前算好；`score_breakdown` 供榜单展开「为什么排第 N」 |
| 官方 | `official.price / price_note / open_hours / close_day / booking_rule / release_time / valid_until / source_url / fetched_at` | 无则空，不得编造 |
| 档案 | `profile.duration_hours / best_time_slot / is_all_day / highlights[] / photo_spots[] / avoid[] / tips[] / cost_items[]` | 现有 build_spot_profile 并入 |
| 空间 | `geo.lng/lat / area`，`legs[]{from,to,mode,cost_min,cost_max,minutes,note}` | legs 为结构化交通（见 F-D5） |
| 时间 | `time_window{open,close,suggest_minutes,best_slot}` | 内部时间模型（§9.2） |
| 决策 | `decision.state / day / slot / order / reason / not_selected_reason` | selected 时 day/slot/order 必填；alternate/dropped 必填可展示原因 |

### 6.3 `FoodDecision`（美食对象，独立于 SpotDecision）

| 字段 | 说明 |
|---|---|
| `food_id / name / FoodType / FoodTag[] / area / nearest_poi_id` | nearest_poi_id 关联最近的行程点，支撑「就近用餐」与动线距离排序 |
| `avg_price / avg_price_nature / price_samples_n` | 人均与样本数；**区分正餐人均与小吃单价，禁止用单个极值当正餐人均** |
| `rating_proxy / positive_density / queue_risk / pitfall[] / signature_dishes[]` | 好评密度、排队风险、避坑、招牌 |
| `heat(同 Spot 子结构) / verify / source_tags / decision.state` | 复用证据与决策结构 |

### 6.4 `OfficialFact` 与三层降级

字段同 `SpotDecision.official`。来源优先级：①仓库种子 `data/official_facts/<城市>.yaml`（人工/社区，Demo 与黄金用例走此层，离线可断言）；②高德 POI（配 Key 时补开放时间/坐标，标 source=amap）；③都无 → 空 + 「待核实」+ UGC 区间占位，**严禁填无来源数字**。每条带 `valid_until`，过期降权且不作亮点。

### 6.5 时间与交通

- `TimeWindow`：内部以小时区间 + 建议时长 + 点间通勤 + 用餐/体力余量做可行性；对外映射上午/下午/晚上。全天型点 `is_all_day=true` 独占一天。
- `Leg`（结构化交通，替代纯文本）：`{from,to,mode,cost_min,cost_max,minutes,note}`；现有 `planner.transport_hints` 必须改为输出该结构（文本只用于渲染），否则开源无高德 Key 的多数场景无法汇总交通费。

### 6.6 预算 `BudgetPlan / BudgetLine`

- `BudgetLine{item, poi_id, kind(rigid/flexible), nature(官方确定/UGC参考/基线估算/待核实), amount, amount_min, amount_max, source_url}`
- `BudgetPlan{tickets[],transport[],meals[],others[],contingency,subtotal,total,vs_budget(结余/匹配/超支),adjustments[]}`；**分项加总恒等于 total（离线可复算）**；门票/交通为 rigid，餐饮为 flexible 单列。

### 6.7 行程 `DayPlan / ItineraryBlock`

- `ItineraryBlock{poi_id, slot, start,end, play_minutes, leg_from_prev(Leg), ticket_summary, on_point{photo_spots[],pitfalls[]}, food_hint(仅引导到美食榜锚点，不排具体店)}`
- `DayPlan{day, blocks[morning,afternoon,evening], day_subtotal, notes[]}`；**每个 block 必须有 poi；相邻 block 必须有 leg；收费点 block 必须有 ticket_summary，取不到标待核实。**

### 6.8 榜单与详情（本版新增，下钻的基础）

- `RankItem{ref_id, rank, score, score_breakdown{}, trend, state(selected/alternate), not_selected_reason, tags[]}`：热度榜 ref 指向 SpotDecision，美食榜指向 FoodDecision。
- `PoiDetail`（catalog）：由 SpotDecision 投影出的完整档案（简介、门票/预约/开放、交通、最佳时段、热度构成、避坑清单、少量评论引用与来源、置信度、Plan B）。
- `FoodDetail`：人均及性质、招牌、品类标签、好评/排队/避坑、最近行程点与步行距离、少量引用与来源。
- **不变量：每个 RankItem.ref_id 必须能在 catalog 中找到唯一详情（R11，无死链，可离线断言）。**

### 6.9 介绍 `IntroSection`

`{title, body, facts_ref[]}`：目的地总览、最佳季节、抵达/市内交通、整体注意事项、入选点速览。`facts_ref[]` 指向其引用的证据对象 id，**保证介绍不另造事实（R12）**。

### 6.10 质量报告 `QualityReport`

`{checks[]{rule_id,name,status(pass/warn/fail),actual,msg}, score, issues[], repair_rounds, unresolved[]}`，必须可展示。规则集 R1~R12 见 §9.5。

### 6.11 顶层聚合 `TripPlan`（决策层唯一产物）

```jsonc
{
  "meta": {"city","days","budget","stay","prefs","collect_mode","generated_at","facts_cutoff","synthetic": false},
  "intro": [ IntroSection ],
  "itinerary": [ DayPlan ],
  "heat_ranking": [ RankItem ],
  "food_ranking": [ RankItem ],
  "catalog": {"poi": {poi_id: PoiDetail}, "food": {food_id: FoodDetail}},
  "budget": BudgetPlan,
  "quality": QualityReport,
  "decision_table": [ 全部候选的来源/证据/热度/状态/理由 ],
  "plan_b": [], "to_verify": [], "appendix": {"sources":[], "disclaimer": ""}
}
```
完整合成示例见附录 B。**Markdown / HTML / API 都只从这一个对象渲染。**

---

## 7. 采集适配层：SourceAdapter、两档采集、三档模式、提速、多源、合规

### 7.1 `SourceAdapter` 抽象接口（Epic B）

统一接口（命名自定，语义必须一致）：
- `capabilities() -> {search, fetch_meta, fetch_comments, official_fact, needs_login}`：声明能力，供编排层决定调用与降级。
- `search(keyword) -> [ref]`；`fetch_meta(ref) -> 元数据`；`fetch_comments(ref, limit) -> [去标识化评论]`。
- 统一返回白名单结构，经 `sanitize` 去标识化后才允许离开采集层。
- **注册机制**：适配器可插拔；平台采集器**默认关闭、用户显式启用并确认合规提示后才工作**；新增一个源只需实现接口 + 在 CONTRIBUTING 文档说明（社区扩展点）。
- AC：用两个假适配器（合成源）离线跑通编排；禁用某源时主流程可降级，不报错中断。

### 7.2 两档采集漏斗（解决 P-13，提速 + 降风控 + 降合规风险）

1. **元数据快扫档（comments=0）**：不展开评论面板，只取视频/条目卡片字段：文案、点赞、**平台评论总数 platform_comment_total**、收藏、分享、发布时间、作者、链接。用于**候选发现 + 热度初筛，全量跑、快、低风控、不采个人信息**。（项目已有 `collect_comments=False` 开关与 heatrefresh 局部实践，将其推广到主行程链路。）
2. **基线收敛**：用热度 + 证据 + 官方硬约束把候选收敛到 Top 8~12。
3. **入围精抓档**：**只对入围点**抓评论，喂验证/避坑/情感。评论总抓取量较「每个候选都抓」下降一个数量级，但避坑能力完整保留。
- **禁止全局 comments=0 到底**：元数据档**只能做发现，不能产出避坑/口碑结论**（评论为空时 extract 无米下锅、情感趋势恒「数据不足」、验证只剩营销文案=让嫌疑人当法官）。
- AC：构造 20 候选夹具，仅 Top N 触发 `fetch_comments`（用调用计数断言其余候选评论抓取次数为 0），最终避坑仍来自入围点评论。

### 7.3 三档采集模式 `CollectMode`（用户可见，默认安全档）

| 档位 | 行为 | 产出边界（报告必须显式标注） |
|---|---|---|
| `heat_only` | 全程元数据，零评论 | 只做热度发现与介绍，**明确标注「本档未做口碑验证，不产出避坑结论」**，避坑区显示该提示而非空白 |
| `balanced`（默认） | §7.2 两档漏斗 | 完整三段式，评论只抓入围点 |
| `deep` | 全量评论（原行为） | 最完整、最慢，启用前提示风控与耗时，用户自担 |
- AC：三档分别可运行；heat_only 报告不出现任何伪造避坑点；模式写入 `meta.collect_mode` 与需求回显条。

### 7.4 热度口径升级（在现有 heat.py 上演进）

现状三因子：互动 40%（点赞 log 归一，anchor 5w）、评论密度 30%（**现为「实际抓到条数/视频数」，是采样值**）、新鲜度 30%（近 90 天视频占比），已有 log 归一与四态趋势，**保留**。升级点：
1. 评论密度改用**平台评论总数**（无采样偏差）；`VideoItem` 需新增平台总评论数字段。
2. 平台总数是累计存量，必须叠加**近期窗口密度**（近 30/90 天视频的评论增量），避免老景点常年霸榜。
3. 新增 `controversy_ratio = 评论数 / 点赞数`：比值异常高往往是争议/吐槽扎堆，作为**「该点优先进入精抓」的触发器**。
4. 多指标交叉防刷（评论/点赞/收藏/分享背离异常则降权并标「数据异常，谨慎参考」），营销号占比沿用。
5. 趋势四态判定沿用 `trend_of`，新鲜度缺失保守计入旧内容。
- AC：用含「刷高评论但零点赞」「近期井喷」「高赞但陈旧」三类夹具，排序与趋势标签符合预期，且全部纯函数可离线测。

### 7.5 抖音提速（继承已完成 + 补齐两项，架构手段而非硬堆并发）

- **已完成勿重做**：监听接口 JSON 取评论/元数据（DOM 降级）、拦重资源、条件等待、多标签 + 预约式令牌桶（间隔不随并发缩短，已有 `test_rate_limiter_global_queue` 断言）。
- **F8.4 视频级增量缓存（待做）**：`Comment` 增 `cid`（评论 ID，非个人可识别信息，需在合规说明写明）并加入白名单；知识库新增视频/评论级表；二次运行**已入库评论不重复做要点提取，只追加新增**（判断有无新评论仍需打开视频页，不做「已采视频永不采集」的错误假设）。
- **F8.6 要点提取结果缓存（优先做，最大成本杠杆，待做）**：现状 `spot_cache` 只存 raw_path，缓存命中仍对每视频重调 LLM（12×5≈60 次，占单任务约六成调用）。按 `video_id + 提取器提示词版本号` 缓存提取结果，提示词版本变更自动失效，缓存内容为白名单产物。
- LLM 侧：要点提取并发在合规前提下适度上调（3→5~8，配合全局限速与成本上限）。
- 要求分阶段耗时埋点（搜索/采集/证据/规划/回炉/渲染分别计时），先 profile 再优化，禁止凭感觉。

### 7.6 多源分工（不是谁替换谁）

| 源 | 适配器 | 定位 | 缺失处理 |
|---|---|---|---|
| 抖音 | DouyinAdapter | **时效情报 / 野生避坑 / 小众与非售票点 / 热度**（balanced 下只精抓入围点） | 失败降级，不影响官方层 |
| 携程 | CtripAdapter（**可选**） | 结构化星级/好中差评、消费后**基线**，补稳定评价 | 默认关闭/可缺，缺了主流程照跑；不做评论主力替换抖音 |
| 官方 | OfficialAdapter（**优先**） | 门票/预约/放票/闭馆/开放，确定事实层 | 走 §6.4 三层降级，宁缺不编 |

- 仲裁序见宪法第 3 条；冲突时以官方为准并保留「评论反馈与官方不一致」的可见提示（F-C3）。
- **明确不做**：不把携程当合规避风港（其评论同为受保护数据集合，且有字体反爬/登录门槛），因此 Ctrip 必须可选、可降级、不得成为跑通前置。

### 7.7 合规护栏（开源必备，特性化实现）

**红线（代码与文档都不得提供）**：不绕过验证码/登录墙/风控（遇到即暂停并提示用户）；不逆向签名/加密参数；不伪造设备指纹；不多账号/代理池/分布式；不抓非公开（私信/后台/好友）数据；不做抓取数据的再分发/出售/竞争性替代。
**默认即合规**：
1. 默认 `balanced` 安全档；保守频控 + 全局限速；单账号；设单次视频数/评论数规模上限。
2. 采集出口即去标识化：字段白名单，不持久化昵称/头像/用户 ID/IP；样例、日志、报告、缓存中零个人可识别信息。
3. 不内置任何 cookie/登录态/Key/真实采集数据；`.gitignore` 覆盖，开源前全 git 历史密钥扫描（F-H5）。
4. 评论原文只做**少量、必要、标来源**的引用（做观点分析，不做语料库搬运）；不提供「批量导出他人评论数据集」。
5. 平台中立表述：README/标题/卖点**不得**出现「免登录爬抖音/突破限制/防封/无限抓取」等定向破解话术；定位为「多源 UGC 分析框架，平台源为用户自担风险的可选项」。
6. 首次启用平台采集器弹合规确认；仓库根放 `DISCLAIMER.md`（要点与可直接使用的文本见 §17.3）；Issue/PR 模板禁止讨论账号/代理/绕过，违规关闭。
- AC：代码库全局检索不到绕过/伪造相关实现；去标识白名单有离线测试；默认配置下所有激进选项关闭。

---

## 8. 数据底座与证据层（Epic A / C）

- **F-A1 证据全量保留（P0，修 P-2）**：verify 的 evidence/pitfall_risk/reason/verdict 全量进 SpotDecision，淘汰点也保留；**禁止在编排层只取 name 丢弃其余字段**。AC：任意候选（含淘汰）都能在中间对象与最终决策表查到结论与理由。
- **F-A2 候选公平截断（P0）**：候选超验证上限时，先按「类别保底（景点优先）+ 基线理由质量 + 热度」排序再截断，城市级地标不会因返回顺序靠后失去验证机会（修现状固定 `cands[:12]`）。AC：地标置于列表末尾的夹具中仍进入被验证集合。
- **F-A3 主体归属强校验（P0，修 P-6）**：任何要点/贴士/避坑/费用并入 POI 前校验主体一致，提到他者归到对应主体或丢弃。AC：「甲档案混乙贴士」夹具输出无乙主体。
- **F-A4 证据前置（P0，修 P-8）**：热度/证据/风险/类别在规划调用前就绪并作为规划输入。AC：高热度强证据夹具中该点必入选或给出硬理由（闭馆/超时/预算/用户排除）。
- **F-C1 官方事实层（P1，三层降级见 §6.4）**：种子 YAML + 高德 + 置空待核实；Demo/黄金用例走种子层离线可断言；社区贡献入口写进 CONTRIBUTING。AC：种子夹具返回值与 YAML 一致并带 source_url；过期事实不进亮点；无来源时不出现任何具体票价数字。
- **F-C2 证据层其余能力保持**：立场提取、置信度、营销号识别、情感趋势（评论不足显式「数据不足」）、避坑置信排序。
- **F-C3 多源冲突仲裁（P1）**：官方 > 多源 UGC，并展示分歧。AC：「评论说免费、官方收费」夹具最终取官方价并展示分歧提示。

---

## 9. 决策层：硬门禁行程 + 预算闭环 + Plan-Check-Repair（Epic D）

### 9.1 行程硬约束（最终形态的强制项）

- 每个时段 block 必有 POI（R1 覆盖密度，按天数定合理下限与体力上限）。
- 相邻 block 必有结构化 Leg 交通（方式/线路/耗时/费用）。
- 收费点必有门票摘要（官方价 + 预约规则 + 放票时间 + 闭馆日）；取不到标「待核实」，**禁止记 0 又在文案声称主要花在此（修 P-3）**。
- 高价值点（热度 TopN 或 evidence=强）未入选必须命中**硬理由闭集**（闭馆冲突/单日时长不足/预算超限/用户排除/需整天而剩余天数不够），自由文本不算通过（R2）。

### 9.2 时间窗可行规划（修 P-4）

- F-D1 内部时间模型：开放/闭园、建议时长、点间通勤、用餐/体力余量；全天型独占一天。
- F-D2 确定性硬校验（不能只靠提示词）：闭馆日不排入；best_slot=晚上必排晚上；单日「游玩+通勤+用餐」不超可用窗；每晚不无故留空。AC：三类违规夹具门禁均判 fail 并触发回炉。
- F-D3 动线：同日空间聚类顺路、酒店往返；跨区折返超阈值 warn 并回炉优化。

### 9.3 餐饮解耦（最终产品决策，根治 P-7）

- 时间线**不排具体餐厅**，block 只给 `food_hint`（就近引导到美食榜锚点）。
- 预算中餐饮为 **flexible 弹性**：按美食榜人均 × 餐次给区间，**显式标注「未指定餐厅，按美食榜人均估算」**；不再出现「美食:无却照算正餐费」。
- 城市正餐基线兜底**只允许「品类 + 人均区间」（如「古城刀削面人均 15~30 元」），严禁编造未调研店名**；不得改弱现有 `_filter_fabricated_food`。
- AC：零餐厅夹具中，时间线无虚构店、餐费为标注清楚的弹性区间且与推荐口径一致；渲染不出现候选清单外店名。

### 9.4 预算约束求解（修 P-3，刚性/弹性拆分）

- F-D4 门票以官方为准、缺口显式（带来源区间占位，不记 0）。
- F-D5 交通结构化（§6.5 Leg），交通费=本行程 Leg 汇总，取消「每点固定 15 元」作为唯一来源；无实测才保守占位并标估算。
- F-D6 只统计入选点、按真实排布出**每日小计**（不跨天机械均摊），分项加总恒等总计。
- F-D7 正餐人均口径：区分正餐/小吃单价，剔除单个极值，按实际入选餐厅统计，不足用城市基线区间并标「基线估算」（修大同 7 元凉粉问题）。
- F-D8 预算作为约束：超支按「砍体验/购物→换低价替代→调餐饮档」顺序调整并记录 adjustments；压不下来显式报缺口，不假装结余。

### 9.5 质量门禁规则集 R1~R12（独立确定性裁判）

| 规则 | fail 条件 |
|---|---|
| R1 覆盖密度 | 入选点低于按天数下限或高于体力上限；某时段无故空缺 |
| R2 高价值零静默丢弃 | TopN/强证据点未入选且理由不在硬理由闭集 |
| R3 时段/开闭园 | 晚上型未排晚上，或排入闭馆日 |
| R4 时间可行 | 单日时长+通勤+用餐超可用窗；同时刻两地 |
| R5 动线 | 同日折返超阈值（warn） |
| R6 预算 | 估算超预算且未做调整；分项加总≠总计（fail） |
| R7 餐饮一致性（本版改写） | 时间线出现未调研店名（fail）；弹性餐费缺标注（fail）；推荐与费用口径矛盾 |
| R8 避坑归属 | 避坑条目不属任何入选/备选点 |
| R9 时效 | 引用过期活动/旧价（valid_until 早于生成日） |
| R10 来源完备 | 入选点缺可溯源来源；收费点缺门票且未标待核实 |
| R11 榜单详情一致（新） | 任一 RankItem.ref_id 在 catalog 无唯一详情（死链）；或详情无对应榜单项 |
| R12 三处同源一致（新） | 介绍/行程/详情中同一事实（票价/时间/避坑）取值不一致 |

- F-D9 有限回炉：fail 项转具体修正指令回炉，**最多 2 轮，只采纳问题数严格变少的版本**。
- F-D10 妥协显式化：仍 fail 进 `quality.unresolved`，在报告「已知妥协/待你定夺」列出原因与可选动作。AC：1 天硬塞 6 远点位的不可满足夹具，报告显式列出而非输出看似正常行程。

### 9.6 时效治理（修 P-5）

- F-D11 所有时间敏感事实带 fetched_at/valid_until，过期降权、不作亮点、必要时触发重核；报告标注「核验日期 + 出发前以官方为准」。AC：生成日晚于活动结束日的夹具中该活动不进推荐。

---

## 10. 攻略介绍模块 What（Epic E，新增）

- **F-E1 目的地总览**：城市/景区简介、最佳季节、抵达枢纽与市内交通、整体避坑与行前必办（预约/放票时间点）。只读证据对象与官方事实，`facts_ref` 挂引用。
- **F-E2 入选点速览**：每个 selected 点一两句简介（叙事化），详情交给 catalog，不与详情大段重复。
- 边界：介绍**不新造任何数字**（票价/时间一律引用 §6 对象）；heat_only 档可出介绍但须标注未做口碑验证。
- AC：介绍中每条事实都能在证据对象找到同源值（R12 夹具）；介绍不出现 catalog 之外的点位；无数据的小节给最小可用版本而非整段缺失。

---

## 11. 双榜与详情库 Explore（Epic F，新增/重构）

### 11.1 热度榜（F-F1）

- 排序：`heat.score`，每项展开 `score_breakdown`（互动/密度/新鲜度各占多少，**可解释**）+ trend + 营销号占比 + state（已入选/备选）+ not_selected_reason（为何没排：闭馆/绕路/时间不够）。
- 下钻：点击 → catalog 对应 PoiDetail。
- AC：榜单分数与展开构成一致且可复算；榜单项与行程状态联动；每个 ref 有详情（R11）。

### 11.2 美食榜（F-F2，独立采集与口径，修 P-12）

- **独立候选与配额**：美食不再蹭景点候选，走独立关键词/类别采集（正餐/小吃/饮品/夜市），保证每个目的地产出足够样本（数量下限写入配置，不足显式说明而非只列 1 家）。
- **排序维度**：好评密度、人均（带性质与样本数）、本地老字号 vs 网红标签、排队风险、避坑、与最近行程点的动线距离（nearest_poi_id）。
- 下钻 → FoodDetail：人均/招牌/品类/排队/避坑/步行距离/引用来源。
- AC：合成夹具下美食榜 ≥ 配置下限或显式标注样本不足；排序分可复算；正餐人均不被小吃极值拉低；每项有详情。

### 11.3 详情库 catalog（F-F3）

- 由统一对象**投影**生成（不二次 LLM 编造）；PoiDetail/FoodDetail 字段见 §6.8；每条含置信度、少量标来源引用、核验日期。
- AC：catalog 与榜单一一对应；详情数字与行程/介绍同源（R12）；无来源字段显式「待核实」。

### 11.4 下钻交互（双端分别实现）

- **HTML**：榜单项为可点击卡片，点击展开详情面板/锚点定位（默认静态可读，展开为增强；不依赖 hover 才能获得核心信息；移动端触控区 ≥44px）。
- **Markdown**：榜单为带锚点目录，详情为详情库小节，点击跳转（GitHub/编辑器锚点）。
- AC：HTML 无 JS 时静态榜单仍完整可读；MD 锚点均可跳转；自动检测无死链（R11）。

---

## 12. 表达层：统一 TripPlan → 双渲染（Epic G，新增核心）

### 12.1 最终报告信息架构（MD / HTML 对齐，自上而下固定）

0. **需求回显条 + 行程概览卡**（沿用现有 build_overview，**不得丢失**：天数/点数/餐厅数/总预算日均/亮点避坑计数/采集档位）。
1. **质量分卡（新）**：总分、各规则状态、回炉轮次、已知妥协数。
2. **攻略介绍（新，What）**：§10。
3. **选点决策表（新，最重要差异位）**：全部候选的来源/证据/热度/营销占比/状态/一句话理由，**淘汰点也展示**。
4. **逐日行程（升级，How）**：时段时间轴、点位徽章、逐点结构化交通、门票（官方+预约+放票+闭馆）、打卡点、内嵌避坑、餐饮仅引导锚点。
5. **预算明细（升级）**：按入选点逐项、刚性/弹性分组、金额性质标注、官方来源链接、每日真实小计、与预算对比及调整记录。
6. **避坑专题（收敛）**：仅入选/备选点，附少量原文与来源。
7. **Plan B 与待核实清单（新）**：固定区块，数据不足给最小可用版。
8. **热度榜（升级为可下钻）**：分数构成可展开、状态联动。
9. **美食榜（新，可下钻）**。
10. **详情库（新，下钻落点）**：PoiDetail / FoodDetail。
11. **合规与时效声明（升级）**：核验日期、「出发前以官方为准」、采集档位、数据来源与去标识化说明、Demo 合成数据声明。

### 12.2 渲染规则

- 决策层产出 `TripPlan` 后，`render_markdown(plan)` 与 `render_html(plan)` **纯函数只读**；API/MCP 返回 `plan.json()`。
- HTML 图表用 ECharts（预算构成、热度对比等），**必须带加载失败的文本/表格降级，不允许空白区块**；移动端可读、不依赖 hover。
- 任一区块数据缺失给「最小可用 + 待核实」版本，不得整块消失或编造填满。
- AC：同一 TripPlan 经 MD/HTML 渲染，关键事实文本一致（快照/夹具断言）；破坏任一统一对象字段时渲染降级而不崩溃。

---

## 13. 功能需求总表（可直接转 Issue）

> 优先级 P0=不做不能开源 / P1=差异化核心 / P2=体验增长。状态：新增 / 继承 v2 / 已完成勿重做。详细 AC 见对应章节。

| ID | Feature | 关键 AC 摘要 | 优先级 | 状态 | 里程碑 |
|---|---|---|---|---|---|
| F-A1 | 验证证据全量保留 | 淘汰点也有结论/理由 | P0 | 继承 | M1 |
| F-A2 | 候选公平截断 | 地标置尾仍被验证 | P0 | 继承 | M1 |
| F-A3 | 主体归属强校验 | 不串档 | P0 | 继承 | M1 |
| F-A4 | 证据前置喂规划 | 高热度强证据必入选或硬理由 | P0 | 继承 | M1 |
| F-B1 | SourceAdapter 抽象+注册 | 假源可跑、缺源可降级 | P1 | 新增 | M3 |
| F-B2 | 两档采集漏斗 | 仅入围点抓评论（计数断言） | P1 | 新增 | M3 |
| F-B3 | 三档 CollectMode | heat_only 不伪造避坑 | P1 | 新增 | M3 |
| F-B4 | 热度口径升级 | 平台总数/近期窗口/争议比/防刷 | P1 | 新增 | M3 |
| F-B5 | 评论/元数据接口化等提速 | 已实测 300s→66s | P1 | **已完成** | M0 |
| F-B6 | 视频级增量缓存(cid) | 已入库评论不重复提取 | P2 | 继承 | M3 |
| F-B7 | 要点提取结果缓存 | 二次运行提取 LLM 调用=0 | P1 | 继承 | M3 |
| F-C1 | 官方事实三层降级 | 种子可断言/过期降权/无则待核实 | P1 | 继承 | M2 |
| F-C3 | 多源冲突仲裁 | 官方覆盖评论且展示分歧 | P1 | 继承 | M2 |
| F-D1/2 | 时间模型+硬校验 | 闭馆/晚上型/超时三夹具 fail | P0 | 继承 | M1 最小/M2 完整 |
| F-D3 | 动线优化 | 折返下降并说明 | P2 | 继承 | M2 |
| F-D4~8 | 预算刚性/弹性/口径 | 分项=总计、官方票、剔除极值、弹性餐费 | P0 | 继承 | M1/M2 |
| F-D9/10 | 门禁回炉+妥协显式 | ≤2 轮只变好、不可满足显式列 | P0 | 继承 | M1 |
| F-D11 | 时效治理 | 过期不进亮点 | P1 | 继承 | M2 |
| F-E1/2 | 攻略介绍 | 同源不造数、不与详情重复 | P1 | **新增** | M4 |
| F-F1 | 热度榜可下钻 | 分数可展开、状态联动、无死链 | P1 | 新增 | M4 |
| F-F2 | 美食榜独立口径 | 样本下限/多维排序/可下钻 | P1 | **新增** | M4 |
| F-F3 | 详情库 catalog | 投影生成、一一对应 | P1 | **新增** | M4 |
| F-G1 | 生成呈现分离+双渲染 | 同一 plan 两端一致、可降级 | P0 | **新增** | M4 |
| F-G2 | 报告 IA 落地 | 0~11 区块齐全 | P0 | **新增** | M4 |
| F-H1 | 离线 Demo（合成数据） | 无 Key/无网 60s 出成品 | P0 | 继承 | M5 |
| F-H2 | README 首屏 | 动图/样例/Demo/合规/二开 | P0 | 继承 | M5 |
| F-H3 | 工程化(pyproject/依赖分组/Docker) | 默认不拉重依赖 | P1 | 继承 | M5 |
| F-H4 | 跨平台+社区文件 | 分层支持承诺、CI 矩阵 | P1 | 继承 | M5 |
| F-H5 | 开源前安全扫描 | 零敏感命中、样例脱敏 | P0 | 继承 | M5 |

---

## 14. 非功能需求

### 14.1 性能预算（以实测 profile 为准）

| 场景 | 现状 | 目标 |
|---|---|---|
| Demo | 无 | 零采集零 LLM，≤60s 出完整报告 |
| 单景点采集（5 视频） | 约 66s（已优化） | ≤120s 不回退即可 |
| heat_only 全城 | — | 仅元数据，显著最快，用于快速发现 |
| balanced 标准档 | 约 11~15 分钟（缓存命中约 1.5 分钟） | ≤8 分钟（靠 F-B6/B7 与解耦，**不靠放宽频控**） |
| 调参重排 | 现会重采 | 已有档案时零采集、≤1 分钟（规划链约 60s；确定性重排 ≤5s 为可选增强，非本期 AC） |

### 14.2 模型成本预算（开源用户自备 Key，成本可见是留存关键）

现状单任务约 95~110 次调用、`core/llm.py` 不读 usage、无上限、欠费识别不全。要求：
1. **可见**：累加每次 token，任务结束输出「调用 N 次 / 约 X 万 token」，配 `LLM_PRICE_IN/OUT` 时给估算金额。
2. **有上限**：`LLM_MAX_CALLS_PER_JOB`（默认 200）/`LLM_MAX_TOKENS_PER_JOB`，超限安全停止并给可操作提示，不输出残缺却看似成功的报告。
3. **不浪费**：欠费/免费额度耗尽/403 类**一律不重试**并翻译成中文提示（补全 `_is_billing` 对 `Free quota exhausted / AllocationQuota.*` 的识别，修现状重试 3 次抛裸英文的问题）。
4. **可省**：`CHEAP_MODE`（跳过评价摘要、回炉≤1、验证上限 12→8），调用量降约四成；README 标明各档调用量/费用区间。
5. **首屏承诺**：跑 Demo 不消耗任何模型额度。
- AC：统计可复算；超限安全停；额度类错误重试计数=0（假异常断言）。

### 14.3 可靠性与降级链

官方缺失→待核实不编；高德不可用→结构化估算并标注→再不行区域排线；接口取数失败→DOM 降级；某源缺失→跳过该源继续；LLM 非法结构→normalize；规划多次失败→显式报错可重试，不输出「看起来成功」的残缺报告。

### 14.4 可测试性

判定/预算/时间/仲裁/归一/榜单一致性/同源一致性全部纯函数、夹具驱动、离线可测；新增功能同步补测试，CI 全绿为合并前提。

### 14.5 跨平台（分层承诺，不过度宣称）

Demo + 决策层 + 表达层：Windows/macOS/Ubuntu 三平台保证可跑（CI 覆盖）。采集层：Windows 优先（现 `crawler/browser.py` 浏览器路径为 Windows 硬编码），macOS/Linux 标「社区验证中」并给浏览器路径配置说明；**不得在 README 承诺三平台采集完整可用**。

### 14.6 安全与隐私

Key 只走 keyring 或被忽略配置，不入仓库/日志；评论去标识化白名单；API 输入校验与防目录穿越（已有，保持）；任务可取消、阶段进度可观测。

---

## 15. 黄金回归用例（每次改动必须全绿，写成离线测试）

### Case A：北京 2 天 · 住三环内 · 1500 元/人（不含大交通住宿）· 体验优先 · 年轻人

1. 环球影城级主题乐园**排入且基本独占一天**；未排入则决策表给硬理由闭集之一，不静默消失。
2. 预算门票含其**官方当季票价档**并带来源；不允许「门票 0、文案说主要花在门票」。
3. 每晚有安排或明确空因；「最佳=晚上」的夜景点（如亮马河）排晚上。
4. 避坑每条都能归属到入选/备选点（R8）。
5. 不出现生成日前已结束的活动/旧价作为亮点（R9）。
6. 选点决策表列出**全部**候选及状态与理由（含淘汰）。
7. 预算分项加总恒等总计，结余/超支结论自洽；门票/交通为刚性、餐饮为弹性单列且带估算标注。
8. 质量分卡可见，不存在未列入 unresolved 的 fail。
9. 每日时间轴在开放时间/时长/通勤上可行，无同时刻两地、无单日严重超时。
10. **（新）介绍中的票价/开放时间与行程、详情三处完全一致（R12）。**
11. **（新）热度榜、美食榜每一项都能在详情库找到唯一详情，无死链（R11）。**
12. **（新）行程时间线不出现具体餐厅名，只有美食榜锚点引导；餐费为标注清楚的弹性区间（R7）。**
13. **（新）同一输入跑 heat_only：报告显式标注「未做口碑验证、无避坑结论」，且不出现任何编造避坑点。**

### Case B：大同 2 天（防预算失真与主体串档）

1. 正餐人均不被单个小吃极值（如 7 元凉粉）拉低，2 天总预算无明显失真极低值。
2. 任一景点档案/避坑不混入其他主体内容。
3. 餐厅不足走「品类+人均区间」基线，推荐与餐费口径一致，且无候选外店名。

### Case C：榜单/详情/同源一致性（合成夹具，新增）

1. 人为删掉某榜单项对应详情 → R11 判 fail。
2. 同一票价在介绍/行程/详情写成三个值 → R12 判 fail。
3. 修复后三项一致、无死链 → 全 pass。

### 三档模式对比指标（每次性能相关改动记录）

对同一城市分别跑 heat_only/balanced/deep，记录：头部点命中率、有效避坑条数、端到端耗时、评论请求量、模型调用量。预期：heat_only 命中与发现正常但避坑=0，用以**用数据证明「元数据只能做第一层、不能全局 comments=0」**，并写进 README 设计说明。

---

## 16. 里程碑与实施顺序

> 原则：先把决策地基与质量下限做对（否则开源即公开处刑），再做最终形态门面，然后性能/多源，最后开源冲刺。工程化（F-H3/H4）可并行提前。

### M0 已完成基线（勿重做）
F-B5 采集接口化提速、规划质量最小闭环、预算部分修复（见 §1.4）。**开工前先跑一遍现有测试与三次提交，核对实际代码状态。**

### M1 决策地基（P0）——先「不出错」
- Epic A（F-A1~A4，按 §5.3 双写过渡，每步测试全绿）
- 质量门禁升级为 R1~R12 骨架（R11/R12 先留接口，M4 接通）、F-D9/D10 回炉与妥协
- F-D1/2 时间硬约束最小版（闭馆/晚上型/单日不超时，Case A #3/#9 依赖，必须提前）
- 预算 F-D4/D6/D7 + 餐饮解耦 F-D9 相关的刚性/弹性拆分（修 P-3/P-7）
- **出口**：Case A 子集（#1/#3/#4/#6/#7/#8/#9/#12）+ Case B 全绿；证据全链路可追踪；QualityReport 在 MD/HTML 可见且无未申报 fail。官方票价/过期类断言（#2/#5）按「空+待核实+不编」即过，完整值顺延 M2。

### M2 正确性与可信门面（P0/P1）——「做得对」
- F-C1 官方事实层、F-C3 仲裁；时间模型完整 + 动线 F-D3；交通结构化 F-D5、预算约束 F-D8；时效 F-D11；可信度表达（决策表/徽章/Plan B/避坑收敛）。
- **出口**：Case A 全集（含 #2/#5）转绿；每个决策可解释、官方可溯源、时间预算自洽。

### M3 采集分层 / 多源 / 性能 / 合规（P1）——「足够快、足够稳、合规」
- F-B1 SourceAdapter（先抽象，DouyinAdapter 平移现有实现）；F-B2 两档漏斗、F-B3 三档模式、F-B4 热度口径升级；F-B6/B7 增量与要点缓存；CtripAdapter（可选）、OfficialAdapter（与 M2 官方层对接）；§7.7 合规护栏落地；分阶段耗时埋点。
- **出口**：Demo≤60s（Demo 本身在 M5，此处先保证管线支持）；重排零采集≤1 分钟；二次运行要点提取 LLM 调用=0；单景点采集不回退（≤120s）且限速节奏不变；默认 balanced、激进项默认关、去标识测试全过。

### M4 最终产品形态（P0/P1）——三段式 + 可下钻（本版核心新增）
- F-G1 生成呈现分离：决策层产出唯一 `TripPlan`，新建 render_markdown/render_html 纯函数。
- F-E1/E2 攻略介绍；F-F1 热度榜、F-F2 美食榜、F-F3 详情库；下钻交互（HTML 展开 + MD 锚点）；F-G2 报告 IA 0~11 落地；接通 R11/R12。
- **出口**：Case A #10~#13 与 Case C 全绿；同一 TripPlan 双渲染关键事实一致；无 JS 时 HTML 静态可读；无死链。

### M5 开源冲刺（P0 for Star）——「被看见、被运行、被传播」
- F-H1 离线 Demo + **合成样例数据**（结构与高质量成品一致，店名/评论/数字全虚构，禁用真实平台 UGC，报告与 README 标注「虚构样例」）。
- F-H2 README 首屏（动图/3 成品图/GitHub Pages 在线样例/30 秒 Demo/合规/二开）。
- F-H3/H4 工程化（pyproject、依赖分组默认最小、Docker/devcontainer）、跨平台 CI 矩阵、CONTRIBUTING（重点教「如何加数据源适配器」「如何补城市官方事实」）、Issue/PR 模板、CHANGELOG、首个 Release、Topics/About。
- F-H5 全 git 历史密钥/登录态/真实数据扫描，清理误入库缓存与二进制；LICENSE(MIT) + DISCLAIMER。
- **出口**：干净环境 60s 看成品；陌生访客 30s 懂价值；零敏感泄漏；三平台分层可跑。

---

## 17. 开源发布、合规声明与简历叙事

### 17.1 发布前 Checklist

- [ ] README 首屏：1 动图 + 3 成品图 + Pages 在线样例 + 30 秒 Demo + 一句话差异化
- [ ] `--demo` / Web「体验样例」零 Key 零网 60s 出成品，样例为合成数据并标注
- [ ] pyproject + 依赖分组（核心/server/asr/dev），默认不装 faster-whisper；版本上限锁定
- [ ] Dockerfile / devcontainer 一把启动；三平台分层文档；CI 矩阵
- [ ] CONTRIBUTING / Issue(bug,feature) / PR / CHANGELOG / Release / Topics / About
- [ ] LICENSE(MIT) + DISCLAIMER.md + 首次启用平台源的合规确认
- [ ] 全历史扫描：无 Key、无浏览器登录态目录、无真实评论/个人信息；`.gitignore` 覆盖 `.env / data/ / browser_profile/`
- [ ] 仓库与文案去定向化（无「爬抖音/突破/防封」话术），卖点为中立分析框架

### 17.2 仓库中立结构建议

主仓库 = 平台中立分析内核 + 手动导入/合成样例/官方种子（无登录即可跑 Demo 与决策链）；平台采集适配器作为**内置但默认关闭、显式启用并确认合规**的可选模块（如担心，可进一步将平台适配器拆独立仓库，主仓库只留接口与文档）。主仓库 README 首屏不依赖任何平台登录态。

### 17.3 `DISCLAIMER.md` 要点（可直接使用）

> 1. 本项目仅用于个人学习、研究与技术交流，面向用户本人合法获取并有权处理的内容。
> 2. 使用须遵守所在地法律法规及目标平台用户协议/robots 规则；**禁止商用、大规模采集、绕过登录/验证码/风控等安全措施、抓取非公开数据，禁止存储/再分发/出售所采集内容，禁止用户画像或侵犯隐私/著作权。**
> 3. 本项目不内置任何账号、登录凭证或平台数据，不提供绕过平台安全机制的能力，遇到登录或安全验证即停止运行。
> 4. 使用者独立承担使用本项目的全部责任与风险，因违规使用造成的后果与开发者无关。
> 5. 如认为本项目侵犯权益，请联系开发者第一时间处理。
>
> README 顶部用一两句话**醒目提示**（不埋文末），首次启用平台源时弹一次确认。

### 17.4 简历叙事（STAR，量化处用真实数据替换）

- 独立设计多源 UGC → 证据融合 → 闭环规划的 AI Agent 管线：浏览器采集 + 立场抽取 + 置信度交叉验证 + 营销号识别 + 官方事实校准，把非结构化评论转化为可溯源旅行知识。
- 设计 **Plan-Check-Repair 决策闭环 + R1~R12 确定性质量门禁**与可解释输出（选点决策表/分数可展开榜单/落选理由/Plan B），解决大模型「看似合理实则漏点/超支/时段冲突/数字打架」问题，黄金用例选点与预算自洽率 100%。
- 设计**统一 TripPlan 数据模型与「生成-呈现分离」架构**，一份结构化产物同时驱动 Markdown/HTML/API，支持榜单下钻详情并保证三处事实同源、无死链。
- 工程化：FastAPI+MCP+CLI 三端、异步任务/取消、知识库缓存与视频级增量、LLM 防御性 normalize、成本统计与上限、离线测试与 CI；采集以「接口结构化取数 + 条件等待 + 全局限速多标签」把单景点采集从约 300s 压到 **66s（约 4.5×）**，评论取数反增至 180 条且全带时间戳。
- 开源工程：零配置 Demo、在线样例、数据源适配器扩展机制、合规护栏（去标识化/默认安全档/责任声明），GitHub 收获 X+ Star（据实填写）。

**Topics**：`llm-agent` `rag` `ugc-mining` `information-extraction` `evidence-fusion` `travel-planner` `fastapi` `mcp` `sentiment-analysis` `browser-automation` `knowledge-base` `python`
**传播钩子**：「专门拆台网红景点的 AI」「100 条真实评论照出谁在营销」「会自我检查并重排、还能点开榜单看证据的 AI 行程规划师」。主受众=做 Agent/RAG/舆情的开发者 + 被网红攻略坑过的年轻旅行者。

---

## 18. 关键权衡记录 ADR（避免实现者反复摇摆）

1. **为何用独立规则门禁而非让 LLM 自检？** 同模型兼运动员裁判不可靠；确定性规则可测、有下限、是简历亮点。
2. **官方事实为何不自动全网抓？** 无统一 API、逐站爬不可维护、LLM 联网数字仍是模型输出；走「种子 YAML + 高德 + 置空待核实」，宁缺不编。
3. **并发为何只到 2~3 标签且全局限速？** 合规可信是卖点，稳定可复现 > 极限速度；预约式令牌桶保证并发不缩短请求间隔。
4. **为何做 Demo？** 「跑不起来」是开源最大流失；零成本看高质量成品是 Star 前提。
5. **决策表为何连淘汰点也展示？** 「有证据地说不」比一味推荐更建信任，是最直观差异。
6. **（新）餐饮为何不进时间线、独立成美食榜？** 吃饭低规划刚性，硬排时间既僵硬又会逼出「无店却算餐费」的假数据；解耦后时间线只保「玩+交通+门票」刚性主线，美食榜承载更大信息量与就近选择。
7. **（新）元数据档为何只做第一层、不全局 comments=0？** comment_count 是热度不是口碑、无极性，全局关评论会让避坑/情感归零、验证只剩营销文案；故「元数据全量快扫发现 + 入围精抓验证」，速度与护城河兼得。
8. **（新）携程为何是补位而非评论主力？** 携程评论是结构化「评价」、与 OTA 同质化，且对非售票体验点覆盖薄、有反爬与登录门槛；抖音「情报」才是差异化，故抖音保情报+热度、携程补结构化基线、官方保硬事实。
9. **（新）为何坚持生成与呈现分离？** 「点榜单看详情」需要结构化数据支撑；一份 TripPlan 驱动多端，才能保证三处事实同源、无死链、可离线测试。
10. **（新）为何默认 balanced 安全档？** 开源项目应「默认合规、激进可选、风险告知」，把合规做成特性与简历加分项，而非依赖用户自觉。
11. **商用换源（付费数据商/平台合作）**：逻辑上是合规终局，但本期非商业，仅因 SourceAdapter 抽象而保留「只换适配器、不动决策内核」的未来可能，不排期。

---

## 19. 术语表

| 术语 | 含义 |
|---|---|
| 三段式 | 攻略介绍(What)→行程规划(How)→热度/美食榜(Explore) 的最终报告形态 |
| 基线 Baseline | 模型常识/检索给出的候选与兜底知识，保正确性下限 |
| 官方事实 | 官方票务/政务/景区的票价、开闭园、预约、时效，三层降级 |
| UGC | 用户评论/视频文案等真实生成内容 |
| 两档采集 | 元数据快扫（发现）+ 入围精抓（验证避坑） |
| 三档模式 | heat_only / balanced(默认) / deep |
| 证据强度 | 强/中/弱，对外唯一一套可信度分级 |
| 质量门禁 | 独立确定性规则裁判 R1~R12，决定通过/回炉/显式妥协 |
| 回炉 Repair | 带问题清单重做，≤2 轮且只采纳严格变好版本 |
| TripPlan | 决策层唯一顶层产物，表达层三端只读它渲染 |
| catalog 详情库 | 榜单项下钻落点，PoiDetail/FoodDetail 集合，poi_id 关联 |
| 刚性/弹性 | rigid=门票交通（必花、官方为准）；flexible=餐饮购物（区间估算） |
| 黄金用例 | 北京/大同/合成一致性夹具构成的回归基线，任何改动必过 |

---

## 附录 A：现状关键问题锚点（供定位，以实际代码为准，勿据此机械改码）

| 文件 | 锚点 | 现状问题 | 本版处理 |
|---|---|---|---|
| `service/trip.py` | 候选固定截断处（原 `cands[:VERIFY_MAX]`） | 地标可能失去验证机会 | F-A2 公平截断 |
| `service/trip.py` | 验证结果只取 name 处 | evidence/reason 被丢弃（P-2） | F-A1 全保留 |
| `service/trip.py` | 规划在前、heat_rows 在后 | 热度不参与决策（P-8） | F-A4 前置 |
| `pipeline/candidates.py` | 类别配额/`VERIFY_MAX`/is_marketing | 截断与营销识别 | 演进保留识别、改截断 |
| `pipeline/planner.py` | `_coverage_issues`/plan_itinerary/build_budget/render_trip | 开环/预算/渲染 | 升级 R1~R12、拆餐饮、双渲染 |
| `pipeline/heat.py` | 评论密度= `sum(len(it.comments))` | 采样值当密度 | F-B4 改平台总数+窗口/争议比 |
| `pipeline/extract.py` | 要点从 `item.comments` 抽取 | comments=0 则无避坑 | 两档漏斗保证入围点有评论 |
| `crawler/douyin.py` | 元数据开关 `collect_comments`、监听接口实践 | 已有雏形 | 推广主链路 + Adapter 化 |
| `core/rate_limiter.py` | 类级令牌桶 | 已线程安全（保持） | 多标签共享，勿改弱 |
| `core/llm.py` | 不读 usage、计费错误识别不全 | 成本不可见、空重试 | §14.2 |
| `crawler/browser.py` | Windows 浏览器路径硬编码 | 跨平台受限 | §14.5 分层承诺 |
| `data/reports/行程_北京_* / 行程_大同_*` | 真实问题成品 | 回归事实来源 | 脱敏后作夹具，不直接入库分发 |

## 附录 B：TripPlan 合成骨架（演示数据结构，数值均为虚构示例）

```jsonc
{
  "meta": {"city":"示例市","days":2,"budget":1500,"stay":"市中心","prefs":["体验优先"],"collect_mode":"balanced","generated_at":"2026-09-05","facts_cutoff":"2026-09-05","synthetic":true},
  "intro": [{"title":"城市速览","body":"……","facts_ref":["poi_demo_park"]}],
  "itinerary": [{
    "day":1,
    "blocks":[
      {"poi_id":"poi_demo_park","slot":"morning","start":"09:00","end":"12:00","play_minutes":180,
       "leg_from_prev":{"from":"酒店","to":"示范乐园","mode":"地铁","cost_min":4,"cost_max":6,"minutes":35,"note":"1号线换乘"},
       "ticket_summary":{"price":528,"nature":"官方确定","booking_rule":"实名预约","release_time":"提前7天20:00","close_day":"无","source_url":"https://example/official"},
       "on_point":{"photo_spots":["城堡正面"],"pitfalls":["节假日排队超90分钟"]},
       "food_hint":"园内简餐较贵，晚餐见美食榜 #food-1"}
    ],
    "day_subtotal":560,"notes":["全天型点，本日不另排远点位"]
  }],
  "heat_ranking":[{"ref_id":"poi_demo_park","rank":1,"score":0.95,"score_breakdown":{"like":0.40,"density":0.30,"fresh":0.25},"trend":"长盛不衰","state":"selected","not_selected_reason":null,"tags":["必去"]}],
  "food_ranking":[{"ref_id":"food_demo_noodle","rank":1,"score":0.81,"state":"alternate","not_selected_reason":"时间线不排餐厅，供就近选择","tags":["本地老字号"]}],
  "catalog":{
    "poi":{"poi_demo_park":{"name":"示范乐园","intro":"……","ticket":{},"transport":{},"heat":{},"pitfalls":[],"evidence":"强","sources":[]}},
    "food":{"food_demo_noodle":{"name":"示范面馆","avg_price":25,"avg_price_nature":"UGC参考","signature_dishes":["招牌面"],"nearest_poi_id":"poi_demo_park","walk_minutes":8}}
  },
  "budget":{"tickets":[{"item":"示范乐园门票","kind":"rigid","nature":"官方确定","amount":528}],"transport":[],"meals":[{"item":"正餐弹性","kind":"flexible","nature":"基线估算","amount_min":60,"amount_max":100,"note":"未指定餐厅，按美食榜人均估算"}],"contingency":0.1,"subtotal":0,"total":0,"vs_budget":"结余","adjustments":[]},
  "quality":{"checks":[],"score":0.92,"issues":[],"repair_rounds":1,"unresolved":[]},
  "decision_table":[{"poi_id":"poi_demo_park","sources":["baseline","ugc_douyin","official"],"evidence":"强","heat":0.95,"state":"selected","reason":"高热度强证据且官方票务明确"}],
  "plan_b":["若排队超预期，舍弃西侧次要项目"],
  "to_verify":["示范夜市营业时间以官方为准"],
  "appendix":{"sources":[],"disclaimer":"演示数据为虚构样例"}
}
```

---

（完。最终设计以此版为准：按 §16 里程碑推进，每个 Feature 对照 AC 与 §13 总表自检，每个里程碑用 §15 黄金用例回归，发布前过 §17.1 清单。）
