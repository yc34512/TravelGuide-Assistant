# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)：主版本号对应不兼容变更，
次版本号对应向后兼容的功能新增，修订号对应向后兼容的问题修复。

_English summary: a Douyin-comment-driven travel itinerary planner. Every claim in the
generated roadbook carries a quoted comment, an independent-source count and a three-level
confidence grade; marketing-account copy is excluded from corroboration, and conflicting
stances are presented side by side rather than averaged away. See `README.md`._

---

## v1.0.0 — 2026-09-17

首个正式版本。项目从最初的"采集 + LLM 汇总"演进为一个完整的本地服务：带溯源的分析内核、
可交付的图文路书、可被智能体调用的接口，以及一套能在 CI 里跑的离线测试。

### 分析内核（本项目的核心差异）

- **多源交叉验证 + 三级量化置信度**：高（≥3 独立来源且无矛盾）/ 中（2 来源或轻微矛盾）/
  低（单源或来源疑似营销号）。**营销号文案不计入印证数**——它是"看起来热闹"的主要来源。
- **对立立场强制并列**：同一话题出现相反说法时不取平均、不静默选边，而是并列呈现双方原话。
- **引用防幻觉做在代码层**：每条要点引用的评论原文必须真实出现在输入数据中，否则在代码层丢弃。
  不依赖提示词约束模型"不要编"。
- **避坑专题**：每条附评论原文与来源视频链接；按置信度降序排列，高置信度在前。
- **真实评价摘要**：好评与差评压缩蒸馏成一分钟读完的摘要，内嵌在景点详情卡中。

### 热度评估

- 热度分 = 点赞 40%（对数归一，防头部拉爆）+ 评论密度 30% + 新鲜度 30%（近 90 天发布占比）。
- 四态趋势判定（本周最火 / 长盛不衰 / 正在降温 / 平稳），基于近 7 天与 60 天以上的内容分布；
  "本周最火"只看新鲜度，避免高赞旧内容被误标。
- 每行附**营销号占比**（≥50% 红色预警）与**评论情感趋势**（近 30 天好评率与更早对比）。
- 榜单来源可区分：刷榜采集 / 行程顺带采集 / 两者混合；时间窗口缺失时显示「—」而非 0。

### 行程规划

- **视频行程草案优先**：先从高赞攻略视频提炼逐日编排草案，经 LLM 审核增删改、对齐天数后
  作为排线主干（"先看热门视频怎么排"），而不是让模型凭空开始。
- 候选圈定 → 逐个抖音验证采集（营销号文案自动过滤）→ 交叉验证筛选 → 逐点调研 → 排线。
- **交通方案分层**：配置高德 Key 时逐路段给实测方案（线路 + 站数 + 票价 + 打车费用，
  1.5 公里内给步行）；未配置则降级为 LLM 估算并明确标注。
- **12 条质量门禁 + 自动回炉**：覆盖密度、高价值点零静默丢弃、时段与开闭园、时间可行、
  动线折返、餐饮、避坑归属、时效、来源完备、榜单无死链、两处同源等。
- **不做预算估算**：估算金额不可靠，报告只给行程与调研到的确定门票价（标注来源），不拼总额。

### 产出物

- **双产物**：Markdown 留档 + HTML 可视化路书（逐日时间线、行程全景图、避坑警示卡、
  景点/餐厅详情弹层、出发前确认清单），离线可打开。
- **攻略报告图文版**：速览三色卡（值得做 / 别踩坑 / 有争议）+ 攻略详解 + 来源要点清单，
  引用编号可点击跳转到对应来源。
- 报告开头回显输入参数，便于对照；同时产出一份「出发前请自行确认」清单（票价、开放时间、
  预约要求、活动档期）——把"哪些事需要你自己核实"显式列出来，而不是假装全都可靠。

### 工程质量

- **246 项离线测试**：纯函数与夹具驱动，不联网、不调 LLM、不开浏览器，约 6 秒跑完。
- **GitHub Actions CI**：每次 push / PR 执行全模块语法检查 + 上述测试；跑在 Ubuntu 上，
  因此也顺带验证了代码不是只在开发机上可运行。
- **知识库与缓存**：SQLite 持久化，关键词自动归一化（"西湖攻略"≈"西湖"），7 天保鲜期内
  命中缓存约 30 秒出报告。
- **任务档案落库**：终态（完成 / 失败 / 取消）持久化，服务重启后历史仍可浏览与下载。
- **防御性降级**：所有 LLM 输出先规范化再使用（结构不合法即降级为安全默认值，不抛裸异常
  中断主流程）；HTML 渲染失败降级文本版；高德不可用降级 LLM 估算。
- **API 安全**：报告下载接口防目录穿越。

### 合规设计

- **分析内核平台中立**：`pipeline/` 只消费统一数据模型，不感知数据来自哪个平台；
  新增数据源只需实现同一套适配器接口。
- **采集源默认关闭（opt-in）**：全新 clone 不发起任何平台采集。启用需显式设置环境变量，
  并由使用者自行承担责任。
- **合规护栏强制生效**：启用 UGC 源前校验频控未被禁用（两个间隔参数均为 0 时**拒绝启用**）；
  请求间隔 2.5~5 秒；评论去标识化（不采集昵称、头像、用户 ID）；低质评论自动过滤。
- **绝不使用绕过技术**：检测到验证码或风控中间页时只停止本次会话的采集并回退缓存/基线，
  不尝试任何验证码破解或签名绕过。
- **仓库不含任何密钥**：API Key 只存在操作系统级凭据管理器（Windows 凭据管理器 / macOS 钥匙串），
  粘贴输入不回显，保存前先做真实调用验证。

### 上手体验

- **Windows 三步**：`install.bat`（建虚拟环境 + 装依赖 + 自检）→ 配一次 API Key
  （启动时若未配置会就地弹出向导）→ `运行服务.bat`（浏览器自动打开）。
- **macOS / Linux 手动路径**已在 README 给出（未在真机充分验证，欢迎反馈）。
- **免安装示例路书**：`examples/` 下有一份完整生成产物（Markdown + 可视化 HTML），
  不需安装任何东西即可看到产品形态。
- **智能体接入三通道**：MCP（10 个工具，stdio；任何工具调用会按需自动拉起本地服务）、
  OpenAPI 规范导入、通用系统提示词。

### 已知限制

- 仅接入**抖音**；小红书等其他平台预留了适配器位，尚未实现。
- 单机单账号顺序采集，约 10 条视频 / 5 分钟；**首次调研一座城市约需 30~60 分钟**
  （已有缓存的点位秒级命中）。
- 采集需要本机有 Chrome / Edge，且首次需用采集小号扫码登录。
- **刻意不做预算估算**，金额口径不可靠。
- 未调研到餐厅时美食榜会缺样本——宁可缺，不编造。

---

### 版本沿革（仅主线）

| 阶段 | 主要变化 |
|---|---|
| 初始 | 抖音采集 + AI 攻略整合（CLI） |
| 产品化 | 服务模式与网页界面、任务取消与历史落库、知识库缓存 |
| 质量 | 多源交叉验证与置信度分级、评论质量过滤、缺口定向补采 |
| 行程 | 行程规划师、热度榜、真实评价摘要、视频行程草案链路 |
| 工程 | 行程决策引擎与质量门禁、开源合规架构、离线测试与 CI |
| 体验 | 控制台与路书视觉重做、详情弹层、出发前确认清单、报告降噪、智能体接入 |
| v1.0.0 | 示例路书与界面预览、图文版攻略报告、热度榜兜底、MCP 按需拉起服务、上手链路补全 |
