# PRD：AI 行程决策引擎升级（开源冲 Star 版）

> 文档性质：产品需求文档（PRD）+ 实施规格说明
> 读者：**AI 编码代理 / 接手开发的工程师**（本文件自包含，可直接据此开发，无需口头补充）
> 版本：v2.0 ｜ 制定日期：2026-09-03
> 产品定位：基于抖音真实评论的**避坑型、可解释** AI 行程规划开源项目
> 本期最高目标：**GitHub 开源、获取大量 Star、成为简历上的硬核个人项目**（非商业项目）

---

## 0. 给 AI 编码代理的协作约定（先读）

1. **只在现有架构上演进，禁止推倒重来。** 现有的分层（`core / crawler / pipeline / service / web / templates`）、任务系统（异步 job/取消/历史落库）、防御性规范化（所有 LLM 输出先 normalize 再使用）、纯函数 + `tests_offline.py` 离线测试、Markdown+HTML 双输出、MCP/FastAPI 三端入口、合规设计（频控/去标识化/凭据管理）都是**被验证过的资产，必须保留并延续其代码风格**。
2. **逐条实现本 PRD，每条 Feature 必须同时交付：功能代码 + 对应离线测试用例。** 测试不依赖网络、不依赖真实 LLM、不依赖浏览器（用夹具数据）。
3. **不确定时的默认决策原则**：结构不合法 → 降级为空/安全默认值，绝不抛裸异常中断主流程；数据缺失 → 显式标注"待核实/未知"，**禁止臆造数字、禁止静默丢弃**；两条规则冲突 → 保正确性下限 > 保体验完整度。
4. **不引入未经本 PRD 批准的重型新依赖**；确需新增，优先标准库或与现有依赖同生态，并在 PR 说明理由。
5. 技术栈固定：Python 3.11、DrissionPage、FastAPI、Jinja2、SQLite 知识库、OpenAI 兼容 LLM 接口；不切换语言/框架。
6. 每完成一个里程碑，必须跑通第 9 章的**黄金回归用例**，并保证既有离线测试全绿。
7. 本文中"规则/判定逻辑"用自然语言与条件式描述，实现方式由你决定，但**对外可观测行为必须与验收标准一致**。

---

## 1. 项目背景与现状

### 1.1 项目是什么

一条"抖音 UGC 采集 → 评论立场提取 → 置信度交叉验证 / 营销号识别 → 景点档案 → 行程规划 → 可溯源路书"的完整 AI Agent 管线，提供 CLI、Web（FastAPI）、MCP 三种使用方式。

### 1.2 已经做得好、必须保持的能力（不要改坏）

- 混合候选：LLM 圈定候选 → 逐个抖音验证 → 交叉验证 keep/drop，含类别配额与数量闸。
- 证据加工：评论立场（推荐/中性/避雷）、置信度分级与来源数、营销号文案识别、热度指数与四态趋势、评论情感趋势。
- 工程质量：**1392 行 / 66 项离线测试用例**、CI（ubuntu-latest + Python 3.11）语法检查 + 离线测试、LLM 输出防御性规范化、任务可取消/可落库/有历史、知识库 7 天缓存、HTML 可视化离线降级、API 输入校验与防目录穿越。
- 合规意识：保守频控、评论去标识化（昵称等个人信息解析即弃）、系统凭据库存 Key、只读取登录可见内容。

### 1.3 已诊断出的核心问题（来自北京 2 天 / 大同 2 天真实成品，本 PRD 要系统性解决）

| 编号 | 问题现象 | 本质 |
|---|---|---|
| P-1 | 北京调研 8 个点只排 4 个，热度第一的主题乐园被静默丢弃 | 规划是"一次成稿开环"，无独立质检与强制回炉 |
| P-2 | 候选验证产出的证据强度/风险/淘汰理由在中途被压成布尔值，规划器看不到 | 证据没有作为统一对象贯通全链路 |
| P-3 | 漏算主题乐园 528 元门票，却写"主要花在环球门票"；大同被 7 元小吃拉低到 2 天 255 元 | 预算只认评论数字、统计口径错误、缺官方事实校准 |
| P-4 | 两晚全空；"最佳时段=晚上"的点被排到下午；不知周一闭馆 | 只有上午/下午/晚上三时段粗颗粒，无时间可行性硬约束 |
| P-5 | 9 月行程仍把 8 月已结束的活动/旧票价当亮点 | 时效事实没有"有效期"概念，不过期、不校准 |
| P-6 | 12 条避坑 9 条属于未入选点；档案出现别景点的贴士 | 避坑/档案与最终选点脱节，缺主体归属校验 |
| P-7 | 只调研到 1 家餐厅，4 个正餐槽全空、渲染"美食：无"，餐饮费却照算 | 餐饮无兜底策略，"内容缺失"与"费用估算"矛盾 |
| P-8 | 热度在规划之后才算，仅用于展示，不参与决策 | 数据生产顺序与决策脱节 |
| P-9 | 浏览器靠 DOM 滚动 + 固定 sleep，单标签串行，标准档耗时长 | 采集通道低效、采集与决策强耦合 |
| P-10 | 开源后访客无截图/无 Demo/无样例，跑通需自备 Key+抖音小号+长等待 | 缺零门槛体验路径，Star 转化漏斗第一关断裂 |

> 上表为 v2.0 定稿时的诊断快照（取自 2026-09-03 11:23 的北京报告）。其中 **P-1/P-3/P-4/P-7 已部分修复、P-9 已基本解决**，详见 §1.4。

### 1.4 已完成基线（实现前必读：避免重做，更要避免改坏）

代码在 v2.0 定稿后已推进三次提交（`4c6ab87` 规划质量 P0 / `c6f1e82` 采集提速 / 采集回归修复）。下表能力**已经存在**，实现对应 Feature 时在其基础上演进，不得推倒重写：

| PRD 编号 | 已实现内容 | 代码落点 | 对应 Feature 的剩余工作 |
|---|---|---|---|
| P-1 / F5.1–F5.2 最小版 | 规划覆盖率检查（排点过少/晚上型错位/地标静默丢弃）+ 带问题清单回炉一次，**只采纳问题数严格变少的版本** | `planner._coverage_issues`、`plan_itinerary` | 升级为独立 R1~R10 规则集与可展示的 `QualityReport` |
| P-3 / F4.1、F4.4 部分 | 预算只计入选点；档案无票价时用点位 cost 兜底；缺票价明示"出发前请核实" | `planner.build_budget_summary`（`tickets_detail` / `tickets_missing`） | 官方票价来源、金额性质标注、每日真实小计（不机械均摊） |
| P-4 / F3.2 部分 | 提示词硬约束：每天至少两时段有安排、`best_time_slot=晚上` 必须排晚上 | `PLAN_SYSTEM` 规则 1 | 闭馆日、单日时长上限、通勤可行性等**确定性**校验（不能只靠提示词） |
| P-7 / F4.6 部分 | 美食保底恢复（餐厅全被评审淘汰时救回证据最强前 2 家，营销号过半不救）；`food` 未命中候选店名即清空（防编造） | `candidates.verify_candidates`、`planner._filter_fabricated_food` | 城市正餐基线区间兜底（**只给品类+人均，禁止编造店名**） |
| P-9 / F8.1–F8.3 | 评论走接口 JSON（DOM 降级）、元数据走详情接口 JSON、重资源拦截、条件等待替代全部固定 sleep、多标签并发 + 全局预约式令牌桶、候选池饱和即砍第二轮搜索 | `crawler/douyin.py`、`crawler/browser.py`、`crawler/tabs.py`、`core/rate_limiter.py` | F8.4 视频级增量缓存、F8.5 调参重排 |

**采集层实测（云冈石窟 5 视频 × 3 标签并发，2026-09-04）**：单景点耗时 约300 秒 → **66 秒**；评论 24 → **180 条**（DOM 路径基线约 100 条）；文案完整率 **5/5**（修复后台标签页渲染节流导致的空文案）；评论精确日期覆盖 **100%**（情感趋势不再"数据不足"）；点赞数为接口精确值；作者回复改用 UID 比对。**F8.1/F8.2 的 AC 已可满足**，F8.3 的节奏断言已有 `test_rate_limiter_global_queue`。

---

## 2. 产品目标与非目标

### 2.1 本期目标（按重要性排序）

1. **质量立得住**：产出的行程在"选点合理性、时间可行性、预算自洽、避坑准确、时效新鲜、来源可溯"六个维度达到可公开展示、经得起访客逐条挑刺的水平。
2. **差异看得懂**：让用户/访客一眼看出它与"通用大模型一次性写攻略"的本质区别——**每个决策有真实评论背书，每个落选讲得出证据，系统会自我校验和回炉**。
3. **开源跑得通**：访客 30 秒看懂（首屏素材）、3 分钟零配置看到成品（Demo 模式）、愿意 Star 的开发者能顺畅二开。
4. **简历讲得出**：沉淀为一个能体现 Agent/RAG/数据管线工程能力的代表作（见第 11 章）。

### 2.2 明确非目标（Out of Scope，本期不做）

- ❊ 不做任何商业化：无付费墙、无会员、无云端 SaaS、无账号体系、无服务器部署运营。
- ❊ 不做多账号/代理 IP 池、不做平台签名逆向作为默认通道（与"合规保守"人设冲突，维护成本高）。
- ❊ 不做小程序/App；不做在线托管服务。
- ❊ 不追求覆盖全国城市的运营化数据，数据由用户按需采集 + 本地知识库沉淀。

### 2.3 成功度量

| 类别 | 指标 | 目标 |
|---|---|---|
| 质量 | 黄金回归用例通过率 | 100%（第 9 章断言全部满足） |
| 质量 | 离线测试 | 全绿，新增能力测试覆盖率不低于现有水平 |
| 体验 | Demo 模式从启动到看到完整报告 | ≤ 60 秒、零 Key、零联网采集 |
| 体验 | 标准档端到端时长 | 通过架构优化较现状显著下降（目标在第 8 章给出） |
| 开源 | README 首屏 | 含 1 个动图 + 3 张成品图 + 在线样例入口 |
| 开源 | 跨平台 | Windows/macOS/Ubuntu 均有可验证启动路径 |

---

## 3. 核心设计原则（架构宪法，所有需求服从于此）

1. **证据是一等公民。** 每个地点从被提名到最终落稿，全程携带同一组证据属性，任何阶段不得把证据压扁成"是/否"或一段自由文本后丢弃结构化字段。
2. **规划必须闭环（Plan → Check → Repair）。** 生成与裁判分离：用确定性规则做质量门禁，不达标带着具体问题回炉，仍不达标必须**显式声明妥协**，禁止静默。
3. **多源分层 + 冲突仲裁。** 三类数据源分工：基线常识保下限、官方事实保硬正确、UGC 保新鲜与真实体感；冲突时按 `官方事实 > 多源高置信 UGC > 单源 UGC > 模型推测` 仲裁，并**可见地标注分歧**。
4. **可信度可感知。** 输出回答三个问题：凭什么去（入选理由+来源）、为什么没去（落选理由）、哪些要自己再确认（待核实清单 + Plan B）。
5. **采集与决策解耦。** 数据采集/沉淀与行程规划分层；改预算、偏好、住宿、天数时，决策层基于已沉淀档案**秒级重算**，不重新采集。
6. **一切 LLM 输出不可信，先 normalize；一切关键结论可降级、可离线复算、可测试。**
7. **合规是特性不是负担。** 提速不得以提高风控风险、绕过平台防护为代价；节奏由全局限速器统一兜底。

---

## 4. 目标总体架构

### 4.1 分层与职责

```
多源输入：模型基线常识 │ 官方权威事实〔新增层〕│ 抖音 UGC（未来可插拔更多数据源）
        │
        ▼
① 数据层  采集 / 缓存 / 增量更新 / 知识库（可预热、可复用，与出行程解耦）
        │
        ▼
② 证据层  立场提取 → 置信度 → 交叉验证 → 热度/情感 → 营销号识别
          产出统一 POI 决策对象（SpotDecision），沉淀为可复用档案
        │
        ▼
③ 决策层  规划 Plan → 质量门禁 Check（确定性规则集）→ 不达标回炉 Repair
          内含：时间窗求解、预算约束求解、动线优化、餐饮匹配
        │
        ▼
④ 表达层  行程路书 + 决策说明书 + 质量分卡 + Plan B/待核实清单
          多端出口：Markdown / HTML 可视化 / MCP / OpenAPI
```

### 4.2 一条纵向总线：统一 POI 决策对象

所有模块读写同一个对象（第 5 章给字段契约），替代当前"各阶段临时拼文本"的做法。新增模块只能扩展该对象字段，不得另起平行结构导致信息再次断流。

**迁移策略（必须遵守，否则与 §0.1"禁止推倒重来"冲突）**：现状是 `profiles` / `spot_points` / `heat` / `pitfall` / `food_profiles` 五个平行 dict 在 `service/trip.py` 里拼装，直接换成单一对象会一次性动到 planner、render、trip、heatrefresh、templates、tests 六处。因此采用**双写过渡**：

1. 新建 `pipeline/decision.py` 定义 `SpotDecision` 与**组装适配层**（从现有五个 dict 反向填充），旧函数签名与返回结构一律不动；
2. 门禁（Epic 5）与决策表（F7.1）**只读新对象**，其余渲染路径继续读旧结构；
3. 逐个模块切到新对象（预算 → 避坑 → 热度 → 详情卡 → 模板），**每切一个保持既有 66 项测试全绿**；
4. 全部切完后再删除旧拼装代码（单独一次提交，方便回退）。

### 4.3 模块边界（给实现者的约束）

- 采集器只负责"取数与去标识化"，不做任何选点/评价决策。
- 证据层只负责"结构化判断"（立场/置信度/热度/营销号/官方事实），不做排程。
- 决策层只消费证据层产物，不直接读原始评论、不直接发起采集。
- 表达层只做呈现，不反向修改结论（发现问题应回传质量门禁，而不是在模板里掩盖）。

---

## 5. 统一数据契约（关键，实现前先对齐）

> 下列为逻辑字段契约，命名可贴合现有代码风格，但字段语义与可空性必须满足。

### 5.1 `SpotDecision`（单个地点的全生命周期对象）

| 字段组 | 字段 | 类型/取值 | 说明 |
|---|---|---|---|
| 标识 | name / category | str / 景点·美食·体验·购物 | 主体名称，全链路以此归并，**严禁跨主体串档** |
| 来源 | source_tags | list：baseline / web_search / ugc / official | 记录它被哪些源提名或佐证 |
| 候选 | candidate_reason | str | 基线圈定理由 |
| 验证 | verify.verdict | keep / drop | 交叉验证结论 |
| 验证 | verify.evidence | 强 / 中 / 弱 | 证据强度 |
| 验证 | verify.pitfall_risk | 低 / 中 / 高 | 避坑风险 |
| 验证 | verify.reason | str | 一句话验证/淘汰理由（**必须保留到最终报告**） |
| 热度 | heat.score / trend / videos / likes / fresh_ratio / mkt_ratio / sentiment | 数值与四态 | 由证据层在**规划之前**算好 |
| 官方 | official.price / open_hours / close_day / booking_rule / fetched_at / valid_until / source_url | 结构化 | 第 6.3 节；无则空，不得编造 |
| 档案 | profile.duration_hours / best_time_slot / highlights / avoid / food / photo_spots / tips / cost_items | 沿用现有结构 | 现有 build_spot_profile 产物并入 |
| 空间 | geo.location / travel_legs | 坐标/通行方案 | 沿用高德或降级估算 |
| 决策 | decision.state | 入选 / 备选 / 淘汰 | 由规划+门禁共同确定 |
| 决策 | decision.day / decision.slot / decision.order | 排程结果 | 入选时必填 |
| 决策 | decision.reason | str | 入选/备选/淘汰的**可展示理由** |

> **两套可信度分级的映射（必须打通，否则 F7.2 会同时展示两种徽章让用户困惑）**：现有代码已有 `conf_level`（高/中/低置信度 + `n_sources`，见 `pipeline/verify.py`），本 PRD 又引入 `evidence`（强/中/弱）。二者定义为：`强 = 高置信度且 n_sources ≥ 3`、`中 = 中置信度`、`弱 = 低置信度（含单源与营销号来源）`。**对外只暴露一套（证据强度徽章）**，`conf_level`/`n_sources` 作为内部字段与详情展示；禁止两套各自维护一套判定逻辑。

### 5.2 `TimeWindow`（内部时间模型，对外仍可显示上午/下午/晚上）

- 以"小时区间 + 建议游玩时长 + 点间通勤时长 + 用餐时长"做内部可行性计算；对外标签映射到上午/下午/晚上。
- 每个入选点携带：开放/闭园时间、建议时长、最佳时段、是否需整天（如主题乐园）。

### 5.3 `QualityReport`（质量门禁产物，必须可展示）

| 字段 | 说明 |
|---|---|
| checks[] | 每条规则：rule_id、名称、状态 pass/warn/fail、实测值、说明 |
| score | 质量总分（规则加权，仅作展示与回归对比） |
| issues[] | 未通过项的自然语言修正指令（喂给回炉） |
| repair_rounds | 实际回炉轮次 |
| unresolved[] | 回炉后仍未解决的"已知妥协"，必须在报告显式列出 |

### 5.4 `BudgetPlan`（预算求解产物）

- tickets / meals / transport / contingency（弹性）/ total，逐项可追溯到具体入选点。
- 每项金额标注性质：`官方确定 / UGC 参考 / 基线估算 / 待核实`。
- 与用户预算对比：结余/匹配/超支，以及为满足预算做了哪些替换（写入决策说明书）。

---

## 6. 功能需求（Epic / Feature）

> 优先级：P0 = 不做不能开源；P1 = 差异化核心；P2 = 体验与增长。每条均给验收标准（AC）。

### Epic 1：证据贯通与统一对象（P0）

**F1.1 候选验证结论全量保留**
- 需求：交叉验证产出的 evidence / pitfall_risk / reason / verdict 全量进入 `SpotDecision`，淘汰点也保留，不得只留名称。
- AC：任意一次行程运行结束后，每个被圈定过的候选（含淘汰）都能在中间对象与最终"选点决策表"中查到其验证结论与理由。

**F1.2 热度/证据前置并喂给规划器**
- 需求：热度、证据强度、风险、类别在规划调用前就绪，并作为规划输入；规划器可区分"强证据高热度点"与"普通点"。
- AC：构造一个"高热度强证据点"夹具，规划结果中该点必然入选或在 decision.reason 给出硬理由（闭馆/超时/预算/用户排除）。

**F1.3 主体归属强校验（解决 P-6）**
- 需求：任何要点、贴士、避坑、费用在并入某 POI 前校验主体一致；来源内容提到他者主体时归到对应主体或丢弃，禁止串档。
- AC：用"甲景点档案混入乙景点贴士"的夹具，输出档案中不得出现乙主体内容；对应离线测试断言通过。

### Epic 2：多源候选与官方事实层（P0/P1）

**F2.1 候选截断公平性（P0）**
- 需求：当候选数超过可验证上限时，先按"类别保底（景点优先）+ 基线理由质量"排序再截断，保证城市级地标不会因返回顺序靠后而连验证机会都没有。
- AC：把地标故意放在候选列表末尾的夹具中，地标仍能进入被验证集合。

**F2.2 官方事实模块（P1；原 P0 下调——无可靠自动获取通道，改为种子数据 + 三层降级）**
- **背景修正（实现前必读）**：景区无统一官方 API，逐站爬官网不可维护；用 LLM 联网搜索得到的"带 URL 的数字"本质仍是模型输出，**等同于本 PRD 禁止的"用模型记忆填数"**。因此本 Feature 不以"自动抓取全网官方数据"为目标，也不得作为 M1 出口的前置。
- 需求（三层降级，逐层可离线验证）：
  1. **仓库内种子事实**：`data/official_facts/<城市>.yaml`，人工/社区贡献，每条含 `name / price / price_note / open_hours / close_day / booking_rule / event_start / event_end / valid_until / source_url / fetched_at`；加载后按 `valid_until` 判过期。**Demo 与黄金用例全部走这一层，离线可断言**。
  2. **高德 POI 详情**（已配置 Key 时）：补开放时间/坐标，写入同一结构并标 `source=amap`（复用现有 `core/geo.py`）。
  3. **两者都取不到** → 字段置空 + 标"待核实" + 用 UGC 区间占位（沿用已有 `tickets_missing` 语义），**严禁填任何无来源支撑的数字**。
- 贡献机制：README 开发者章节说明"如何为一个城市补官方事实"，作为社区贡献入口（与 F9.4 的"如何加数据源适配器"并列）。
- AC（全部离线可测）：①给定种子 YAML 夹具，返回的票价/闭馆日与 YAML 一致并带 `source_url`；②`valid_until` 早于生成日期时该条事实被降权且不出现在亮点（联动 F6.1）；③无种子且无 Key 时字段为空且标"待核实"，断言输出中不出现任何具体票价数字。

**F2.3 多源冲突仲裁（P1）**
- 需求：UGC 与官方事实冲突时（如票价、开放时间），以官方为准，同时在该点保留一条"评论反馈与官方信息不一致"的提示；官方缺失时回退多源高置信 UGC。
- AC：构造"评论说免费、官方显示收费"夹具，最终采用官方价并展示分歧提示。

### Epic 3：时间窗可行规划（P0）

**F3.1 时间模型升级**
- 需求：内部以时间窗计算单日可行性：开放/闭园约束、建议时长、点间通勤、用餐时长、合理体力余量；对外仍呈现上午/下午/晚上。
- AC：给定"全天型点 + 两个普通点"的夹具，全天型点独占一天，不与其他点同天硬塞。

**F3.2 硬约束校验（解决 P-4）**
- 需求：闭馆日不得排入；best_time_slot=晚上 的点必须排晚上；单日总时长（游玩+通勤+用餐）不得超过当天可用时间窗；每晚上时段原则上不得无故留空。
- AC：构造"晚上型点被排下午""周一闭馆点排周一""单日时长溢出"三类夹具，质量门禁均判 fail 并触发回炉。

**F3.3 动线优化**
- 需求：同日点按空间聚类、顺路排布，酒店出发并回到酒店附近；跨区域折返超过阈值时判 warn 并在回炉中优化。
- AC：给定东西对撞的点集合，优化后同日折返次数下降且在报告说明动线逻辑。

### Epic 4：预算约束求解（P0，解决 P-3/P-7）

**F4.1 票价以官方为准、缺口显式化**
- 需求：门票优先取官方事实；UGC 数字仅作补充；无任何可靠票价时标"待核实"并给**带来源的区间占位**，不得记 0 又在文案声称"主要花费在此"。
- AC：主题乐园夹具的预算门票必须等于官方当季票价档，且明细可点到来源。

**F4.2 餐饮人均口径修正**
- 需求：区分正餐人均与小吃单价，不得用单个极值（如 7 元小吃）充当全部正餐人均；按**实际入选餐厅**统计；餐厅调研不足时用"城市正餐基线区间"兜底并明确标注"基线估算"。
- AC：大同夹具备餐人均不得被单个小吃极值拉低；2 天总预算不得出现明显失真的极低值。

**F4.3 交通费用与自身路线打通**
- 需求：市内交通优先汇总本行程已生成的通行方案费用；无实测时才用保守占位并标注估算；取消"每点固定 15 元"的机械口径作为唯一来源。
- **前置改动（PRD 补充）**：现有两条交通路径只有一条是结构化的——`geo.route_advice`（高德）返回票价/打车费数值，而 `planner.transport_hints`（无 Key 降级，**开源访客的多数场景**）返回的是自由文本（"打车约 30-50 元"），无法汇总。要做本 Feature 必须先把 `transport_hints` 改为结构化 JSON 输出（每段 `{from, to, mode, cost_min, cost_max, minutes, note}`），文本只用于渲染。
- AC：存在通行方案（高德或结构化估算）时，交通合计等于方案费用汇总而非点数×常数；两者都无时才用占位值且标"估算"。

**F4.4 只统计入选点 + 按真实排布出每日小计**
- 需求：预算口径与最终入选 plan 严格一致；每日小计按当天真实点位与餐食计算，不做跨天机械均摊。
- AC：分项加总恒等于总计（可离线复算）；两天行程的每日小计可不同且与当日安排一致。

**F4.5 预算作为约束求解（P1）**
- 需求：给了总预算时，超支按"先砍体验/购物 → 换低价替代 → 调整餐饮档"的顺序调整，并把每次调整写入决策说明；无法压到预算内时显式告知缺口，不假装结余。
- AC：低预算夹具下，结果不超预算且决策说明列出被替换/移除项；高预算夹具下不无谓砍体验。

**F4.6 内容与费用一致性（解决 P-7）**
- 需求：不允许"美食推荐为空却计入完整餐费"。要么补齐推荐（含基线兜底），要么相应费用按空缺折算并说明。
- **硬约束（与已实现的防编造过滤一致，不得绕过）**：基线兜底**只允许给"品类 + 人均区间"**（如"古城内刀削面人均 15~30 元"），**严禁出现未经调研的具体店名**；`planner._filter_fabricated_food` 会清空未命中候选清单的店名，实现兜底时不得为绕过它而改弱该过滤。
- AC：餐厅为 0 的夹具中，餐食推荐与餐费口径一致不矛盾；且渲染结果中**不出现任何候选清单外的店名**（离线断言）。

### Epic 5：规划闭环与质量门禁（P0，本项目的技术核心）

**F5.1 独立质量门禁**
- 需求：规划产出后，由**独立于生成模型的确定性规则集**逐条裁判，产出 `QualityReport`。规则至少包含：

  | 规则 | 判定（fail 条件） |
  |---|---|
  | R1 覆盖密度 | 入选点数低于"按天数的合理下限"或高于体力上限 |
  | R2 高价值点零静默丢弃 | 热度 TopN 或 evidence=强 的点未入选，且 `decision.reason` 不属于**硬理由闭集**（闭馆日冲突 / 单日时长不足 / 预算超限 / 用户明确排除 / 需整天而剩余天数不够）——自由文本理由不算通过 |
  | R3 时段/开闭园 | 晚上型未排晚上，或点排入其闭馆日 |
  | R4 时间可行 | 单日时长+通勤+用餐超出可用时间窗 |
  | R5 动线 | 同日折返超阈值（warn） |
  | R6 预算 | 估算超预算且未做调整 |
  | R7 餐饮 | 某天午/晚餐空缺且无兜底说明（兜底=品类+人均区间，**编造店名不算兜底、直接判 fail**） |
  | R8 避坑归属 | 避坑条目不属于任何入选/备选点 |
  | R9 时效 | 引用了已过期活动/旧价（见 Epic 6） |
  | R10 来源完备 | 入选点缺可溯源来源 |

- AC：对每条规则分别构造正反夹具，fail/warn/pass 判定正确，且有离线测试。

**F5.2 有限次回炉**
- 需求：将 fail 项转成具体修正指令回炉重排，最多 2 轮；每轮只接受"问题数严格变少"的版本（变好才采纳）。
- AC：注入一个首版违规、二版可修复的夹具，最终采用修复版并记录 repair_rounds；无法修复时进入 unresolved。

**F5.3 妥协显式化**
- 需求：回炉后仍 fail 的项进入 `unresolved`，在报告"已知妥协/待你定夺"区列出，配原因与可选动作，绝不静默。
- AC：构造不可满足夹具（如 1 天硬塞 6 个远距离点），报告显式列出无法满足项，而不是输出一份看似正常的行程。

### Epic 6：时效治理（P1，解决 P-5）

**F6.1 事实有效期**
- 需求：所有时间敏感事实（票价、展览/活动、营业时间、预约政策）带采集时间与有效期；过期自动降权、不得作为亮点，必要时触发重新核验。
- AC：生成日期晚于活动结束日期的夹具中，该活动不出现在亮点/推荐，只可作为"已结束"提示或被剔除。

**F6.2 新鲜度提示**
- 需求：报告标注关键事实的"核验日期"，并对临近变动的信息提示"出发前以官方为准"。
- AC：每条官方事实渲染处可见核验日期与来源。

### Epic 7：可信度表达层（P1，差异化门面）

**F7.1 选点决策表（新增章节，最重要的展示位）**
- 需求：行程开头新增"选点决策"表，逐点列出：名称、来源（基线/官方/UGC）、证据强度、热度与趋势、营销号占比、最终状态（✅入选/⏸备选/❌淘汰）、一句话理由。**淘汰点也展示**——"为什么没带你去 X"是核心差异点。
- AC：夹具中被淘汰点在该表可见且理由来自 verify.reason，不丢失。

**F7.2 点位可信度徽章**
- 需求：每个入选点标题旁展示徽章，如热度趋势、证据强度与来源数、风险等级。
- AC：渲染结果中每个入选点至少携带证据/热度徽章，数据来自统一对象而非临时估算。

**F7.3 决策说明书与 Plan B**
- 需求：说明预算如何匹配、为预算/时间做了哪些取舍；给出 Plan B（雨天、排队超预期、放弃某远点位时的替代动线）与"待核实清单"。
- AC：每份报告固定包含决策说明、Plan B、待核实清单三个区块（数据不足时给出最小可用版本，不可整段缺失）。

**F7.4 避坑只讲本行程**
- 需求：避坑专题默认只收录最终入选/备选点；未入选点的避坑如确有价值，收入"备选点风险"分区，避免用户读到与行程无关的内容。
- AC：北京类夹具中避坑条目 100% 可归属到入选/备选点。

### Epic 8：采集性能与解耦（P1/P2，解决 P-9，架构手段而非硬堆并发）

> 原则：优先"减少无效等待与无效渲染"，再谈并发；提速不得突破全局限速与合规节奏。

**F8.1 结构化取数替代 DOM 滚动（P1）**
- 需求：评论优先通过监听页面自身发出的数据接口响应获取结构化数据（页面自带合法请求，不做签名逆向），替代"滚动→固定睡眠→全量重扫 DOM"；保留 DOM 方案作为降级。
- AC：同一夹具下，结构化通道与 DOM 通道产出的评论集合一致（去标识化后），且结构化通道不再依赖固定秒级睡眠。

**F8.2 资源拦截与条件等待（P1）**
- 需求：非语音转写场景拦截图片/字体/媒体等无关资源加载；所有固定等待改为"等待目标元素/数据包出现或超时"的条件等待。
- AC：关闭 ASR 时页面不加载被拦截资源类型；网络快时实际等待时间显著小于原固定值，网络慢时仍能正确等待。

**F8.3 适度多标签并发 + 全局限速（P2，已实现）**
- 需求：单浏览器内以少量标签并发处理不同视频，**所有标签共享一个线程安全的全局节奏控制器**，保证对平台的总体请求节奏仍接近单用户正常浏览。
- 实现现状（与代码对齐）：`CRAWL_TABS` **默认 3**（环境变量可配，建议上限 3；设 1 即完全回到串行旧行为）；`core.rate_limiter.RateLimiter` 为**类级共享的预约式令牌桶**（每次导航前在全局时间轴上预约一个槽位，后到线程排队尾），因此并发度提高不会缩短请求间隔；`crawler/tabs.py` 每个标签一把锁，同一页面任意时刻只被一个线程驱动。
- AC：并发模式下全局请求间隔受控（已有 `test_rate_limiter_global_queue` 断言 4 线程并发总耗时 ≥ 3×间隔）；任一标签异常不影响其他标签与主流程（已有 `test_fetch_videos_retry_and_cancel`）；**后台标签渲染节流不得影响取数完整性**（已由"元数据走详情接口 JSON"解决，见 §1.4）。

**F8.4 视频级增量缓存（P2）**
- 需求：缓存粒度从"整景点全有/全无"细化到"视频 + 评论 ID"，强制刷新时只补增量评论。
- **前置改动（PRD 补充，实现前必读）**：现有 `Comment` 模型与 `clean_comment` 白名单**不含评论 ID**（当前按正文文本去重），要做增量必须：①`Comment` 增 `cid` 字段并加入白名单（评论 ID 非个人可识别信息，需在合规说明里写明理由）；②知识库新增视频/评论级表并做迁移。另：原 AC 中"已采视频不重复采集"表述不准确——**要判断有无新评论必须重新打开视频页**，准确目标是"已入库评论不重复做要点提取，只追加新增"。
- AC：二次运行同一城市时，已入库评论不重复进入 LLM 提取（调用数下降可断言），仅新增评论参与提取；知识库中可查到增量记录。

**F8.5 决策与采集解耦、调参快速重排（P2）**
- 需求：在已有 POI 档案基础上，修改天数/预算/偏好/住宿/指定点时，直接走决策层与表达层重出方案，**不触发采集**；Web 端提供"基于本次调研快速重排"入口。
- **口径修正（原"秒级重算"不可达）**：规划与回炉都是 LLM 调用，实测规划链约 60 秒（交通估算 35 秒 + 规划与渲染 27 秒）。本 Feature 目标口径改为：
  - **不重采**（硬指标，可断言：重排过程零浏览器动作）；
  - **总耗时 ≤ 现状的 1/5**（现状=重新调研+规划；重排=仅规划链，约 1 分钟内）；
  - 若后续实现"确定性重排"（跳过 LLM，仅按门禁规则微调既有 plan），再单独标 ≤5 秒，**作为可选增强而非本期 AC**。
- AC：已有档案时重排不产生任何采集动作（离线断言采集函数零调用），耗时不含采集与证据加工阶段，且同样经过质量门禁。

**F8.6 要点提取结果缓存（P1，新增——当前最大的模型成本杠杆）**
- **现状问题（代码已核实）**：知识库 `spot_cache` 只存 `raw_path`（原始采集 JSON），**不存要点提取结果**。因此即使 7 天内缓存命中、完全不采集，`extract_points` 仍会对每条视频重新调一次 LLM：12 候选 × 5 视频 = **60 次调用，占单次行程任务总调用量（约 100 次）的六成**。缓存目前只省浏览器时间，不省模型费用。
- 需求：新增按 `video_id` + 提取器提示词版本号缓存要点提取结果，命中则跳过 LLM；档案/评价摘要/语义聚类同理按"输入要点哈希"缓存。
- AC：①同一关键词二次运行，`extract_points` 的 LLM 调用数为 0（用计数器夹具离线断言）；②提示词版本号变更时缓存自动失效；③缓存内容为白名单产物，不含个人可识别信息。

### Epic 9：零门槛 Demo 与开源工程基建（P0，直接决定 Star，解决 P-10）

**F9.1 离线 Demo 模式（P0）**
- 需求：内置 1~2 套样例数据 + 已固化证据对象，提供 `--demo` 与 Web 端"体验样例"入口：无需 LLM Key、无需抖音登录、无需联网采集，即可跑通决策层+表达层，产出完整 Markdown 与 HTML 报告。
- **能力边界（必须写清，否则实现到一半会发现跑不通）**：规划（`plan_itinerary`）与回炉都是 LLM 调用，零 Key 环境下无法执行。Demo 的做法是**回放固化的 plan 与证据对象**，只实跑"质量门禁（确定性规则）→ 预算/时间复算 → 渲染"这三段；**回炉在 Demo 下禁用**（或走夹具 mock），报告需标注"演示模式：规划结果为固化样例"。
- **数据合规（PRD 补充）**：Demo 与 GitHub Pages 在线样例**不得使用真实采集的评论内容**（即便已去标识化，公开仓库 + 静态托管等于二次分发平台 UGC，与 §8.2 合规定位冲突）。样例须为**合成数据**（虚构但贴近真实：店名/评论/数字均自造），并在报告顶部与 README 标注"演示数据为虚构样例"。真实数据只留在用户本地知识库。
- AC：在干净环境（无 .env、无网络、无 Key）下一条命令/一次点击在 60 秒内得到完整成品报告；仓库内样例数据带 `synthetic` 来源标记，不含真实平台 UGC 原文（离线断言）。

**F9.2 README 首屏改造（P0）**
- 需求：README 顶部 30 秒讲清价值，顺序固定为：① 一张端到端动图（输入→实时进度→可视化路书/决策表/避坑/热度）② 一句话差异化定位 ③ 三个在线样例报告入口（GitHub Pages 静态托管 HTML）④ 30 秒 Demo 跑法 ⑤ 安装与完整使用 ⑥ 架构与技术亮点（面向开发者）⑦ 合规声明 ⑧ 二次开发指南。实现细节（凭据管理/MCP 等）下沉到开发者章节，不占首屏。
- AC：用"陌生访客 30 秒内能否说清它是什么、强在哪、怎么试"作为验收；首屏无大段实现细节。

**F9.3 标准 Python 工程化（P1）**
- 需求：提供 `pyproject.toml`，依赖分组（核心 / server / asr / dev），默认最小安装；锁定主要依赖版本上限；提供 Dockerfile / devcontainer 实现"一把启动"。
- AC：全新环境按 README 可完成安装与 Demo；`pip install` 默认不拉取语音转写等重依赖。

**F9.4 跨平台与社区基建（P1）**
- 需求：补齐 macOS/Ubuntu 启动路径并在 CI 增加导入级冒烟；新增 CONTRIBUTING（重点写"如何加一个数据源适配器"与"如何为一个城市补官方事实"）、Issue（bug/feature）模板、PR 模板、CHANGELOG、首个带说明的 Release、合适的 GitHub Topics 与 About 一句话。
- **口径修正（避免给自己挖 issue）**：`crawler/browser.py` 的浏览器候选路径全是 Windows 硬编码（`C:\Program Files\...`），macOS/Linux 只能依赖 DrissionPage 自动查找，且需用户自行处理浏览器可执行文件与登录态。因此跨平台承诺应**分层写明**：Demo + 决策层 + 表达层——三平台保证可跑（CI 覆盖）；采集层——Windows 优先，macOS/Linux 标注"社区验证中"并给出浏览器路径配置方法；不要在 README 里承诺"三平台完整可用"。
- AC：三平台均有文档化启动路径（采集层可标注支持等级）；仓库根具备上述社区文件；CI 在 ubuntu 上跑通导入级冒烟与离线测试（现已有），并补一个非 Windows 矩阵项。
- **附带修正（已有实据）**：`requirements.txt` 将 `faster-whisper` 作为**无条件依赖**，CI 每次都安装它（拖慢且体积大），F9.3 的依赖分组因此是必需项而非镀金。

**F9.5 开源前安全检查（P0）**
- 需求：公开仓库前确认全部历史提交中从未包含 API Key、浏览器登录态目录、原始未脱敏评论；用密钥扫描工具全量检查；.gitignore 覆盖敏感路径；样例数据全部脱敏。
- AC：扫描结果零敏感命中；样例数据中不含任何可识别个人信息。

---

## 7. 输出物信息架构（Markdown / HTML 对齐）

一份成品行程报告自上而下固定包含：

1. **需求回显条**：城市/天数/住宿/预算/偏好/模式（沿用现状）；紧接**行程概览卡**（沿用现状：总天数 / 景点与餐厅数 / 总预算与日均 / 亮点与避坑计数，由 `build_overview` 产出）——**重构模板时不得丢失这两个已有区块**。
2. **质量分卡（新增）**：质量总分、各规则状态、回炉轮次、已知妥协数。
3. **选点决策表（新增，F7.1）**：全部候选的来源/证据/热度/营销占比/状态/理由。
4. **逐日行程（升级）**：小时级时间轴（内部时间窗、对外时段标签）、点位徽章、逐点交通、费用、避坑、餐饮、评论原文引用。
5. **预算明细（升级）**：按入选点逐项、金额性质标注、官方来源链接、每日真实小计、与用户预算对比与调整记录。
6. **避坑专题（收敛）**：仅入选/备选点，附原文与来源。
7. **Plan B 与待核实清单（新增）**。
8. **热度榜（保留并与决策状态联动）**：每个点标注"已入选/备选/淘汰"，不再是与行程无关的孤立榜单。
9. **景点/餐厅详情卡（保留，补官方事实与核验日期）**。
10. **合规与时效声明（升级）**：事实核验日期、"出发前以官方为准"、数据来源与去标识化说明。

HTML 版与 Markdown 信息一致；图表库加载失败时按现有方式降级为文本/表格，不允许空白区块。

---

## 8. 非功能需求

### 8.1 性能预算（端到端，参考目标，最终以实测 profile 为准）

| 档位 | 现状（2026-09-04 实测） | 目标（可验收口径） |
|---|---|---|
| Demo | 无 | 零采集零 LLM，≤60 秒出完整报告 |
| 单景点采集（5 视频×100 评论） | **66 秒**（优化前约 300 秒） | ≤120 秒（不回退即可，余量给慢网络） |
| 快速档（3 视频/点） | — | 端到端 ≤3 分钟 |
| 标准档（12 候选验证 + 规划） | 约 11~15 分钟（缓存命中约 1.5 分钟） | ≤8 分钟（靠 F8.6 要点缓存 + F8.5 解耦，**不靠放宽频控**） |
| 调参重排 | 现状会重采 | 已有档案时 ≤1 分钟且零采集动作（见 F8.5 口径修正） |

要求在各阶段输出耗时日志（搜索/采集/证据/规划/回炉/渲染分别计时），便于持续定位瓶颈，避免凭感觉优化。

### 8.2 合规与安全

- 保持保守频控与"正常浏览节奏"，并发由全局限速器统一约束；不实现风控绕过、不破解签名作为默认路径。
- 评论在解析入口即完成去标识化；样例/日志/报告中不得出现个人可识别信息。
- Key 只走系统凭据库或被忽略的本地配置，不进入仓库与日志。

### 8.3 可靠性与降级链

- 官方事实缺失 → 标待核实，不编造；
- 高德不可用 → LLM 交通估算并标注 → 再不行纯区域排线；
- 结构化取数失败 → 回退 DOM 通道；
- LLM 返回非法结构 → normalize 为安全默认；规划多次失败 → 显式报错与可重试，不输出残缺却"看起来成功"的报告。

### 8.4 可测试性

- 所有判定规则、预算计算、时间可行性、仲裁、归一化均为可离线单测的纯函数；
- 用夹具驱动，禁止测试依赖外网/真实模型/浏览器；
- 新增功能同步补测试，维持 CI 全绿作为合并前提。

### 8.5 成本预算（模型调用，新增——开源访客自备 Key，成本可见性是留存关键）

**现状（代码已核实）**：单次标准档行程任务约 **95~110 次模型调用**，构成为：候选圈定与美食候选 2 次、验证阶段要点提取 12 候选 × 5 视频 = **60 次**、交叉验证评审 1 次、置信度语义聚类 ~9–12 次、景点档案与评价摘要各 ~9–12 次、规划 1~2 次（含回炉）、交通估算 1 次。而 `core/llm.py` **完全没有读取 `resp.usage`**，跑完无从得知消耗；也没有任何单任务调用上限。

要求：

1. **可见**：每次调用累加 prompt/completion token，任务结束在日志与 `result.stats` 输出"模型调用 N 次、约 X 万 token"；配置单价（`LLM_PRICE_IN/OUT`，元/百万 token）时同时给出估算金额。
2. **有上限**：`.env` 提供 `LLM_MAX_CALLS_PER_JOB`（默认 200）与 `LLM_MAX_TOKENS_PER_JOB`；超限**安全停止**并给可操作提示，不得输出"看起来成功"的残缺报告（与 §8.3 一致）。
3. **不浪费**：账户/额度类错误（欠费、免费额度耗尽、`AllocationQuota.*`、403）**一律不重试**并翻译成中文可操作提示。**现状缺陷**：`core/llm.py::_is_billing` 只匹配 `Arrearage/overdue/欠费`，认不出 `Free quota exhausted` / `AllocationQuota.FreeTierOnly`，导致这类永不可能成功的错误被重试 3 次，且向用户抛裸英文 403。
4. **可省**：提供 `CHEAP_MODE`（跳过评价摘要、回炉最多 1 轮、验证候选上限 12→8），调用量下降约四成；README 明示各档位大致调用量与费用区间。
5. **首屏承诺**：README 与 Demo 入口明确写"跑 Demo 不消耗任何模型额度"。

AC（离线可测）：①任务结束输出调用次数与 token 统计且可复算；②超过上限时安全停止并给出提示；③额度类错误进入"不重试"分支（用假异常断言重试计数为 0）。

---

## 9. 黄金回归用例（每次改动必须全绿）

### Case A：北京 2 天 · 住三环内 · 预算 1500 元/人（不含大交通住宿）· 体验优先 · 年轻人

必须同时满足：

1. 高热度主题乐园（环球影城级别）**被排入且基本独占一天**；若未排入，决策表必须给出闭馆/超时/预算/用户排除之一的硬理由，不得静默消失。
2. 预算门票包含该主题乐园的**官方当季票价档**并带来源；不得出现"门票 0、文案却说主要花在门票"的矛盾。
3. 每天晚上时段有合理安排或给出明确空着的理由；"最佳时段=晚上"的夜景点排在晚上。
4. 避坑专题每一条都能归属到最终入选或备选点。
5. 不出现生成日期之前已结束的活动/展览/旧票价作为推荐亮点。
6. 选点决策表列出**全部**候选及其入选/备选/淘汰状态与理由。
7. 预算分项加总恒等于总计（离线可复算），且与用户预算的结余/超支结论自洽。
8. 质量分卡对外可见，且不存在未被列入"已知妥协"的 fail 项。
9. 每日时间轴在开放时间、时长、通勤上可行，不出现同一时刻两地、单日严重超时。

### Case B：大同 2 天（防回归：预算失真与主体串档）

1. 正餐人均不得被单个小吃极值拉低，2 天总预算不得出现明显失真的极低值。
2. 任一景点档案/避坑不得混入其他景点或餐厅的内容。
3. 餐厅不足时走基线兜底，餐食推荐与餐费口径一致。

> 实现要求：把上述断言写成离线测试（用脱敏夹具），进入 `tests_offline.py` 或同级测试集，成为后续所有改动的回归基线。

---

## 10. 里程碑与推荐实施顺序

> 顺序原则：先把"决策地基和质量下限"做对（否则开源即公开处刑），再做差异化门面，然后做性能，最后做开源冲刺；其中开源工程基建（F9.3/F9.4）可与开发并行提前。

### M1 决策地基（P0）——让结果"不出错"
- F1.1–F1.3 证据贯通与统一对象（**按 §4.2 双写过渡策略推进，每一步保持既有 66 项测试全绿**）
- F2.1 候选截断公平
- Epic 5 质量门禁 + 有限回炉 + 已知妥协显式化（在已有 `_coverage_issues` 上升级为 R1~R10）
- **F3.2 时间窗硬约束最小版（从 M2 提前）**：闭馆日不排入、晚上型必须排晚上、单日"游玩+通勤+用餐"不超可用时间窗——**Case A #3/#9 依赖它，留在 M2 会让 M1 出口永远绿不了**
- F4.1/F4.2/F4.4/F4.6 预算口径纠错
- 第 9 章黄金用例：**Case A 子集（#1 #3 #4 #6 #7 #8 #9）+ Case B 全部**转绿
- **出口标准**：上述子集断言全过；证据对象全链路可追踪；`QualityReport` 在 MD/HTML 可见且无"未列入已知妥协的 fail 项"。
- **验收边界说明**：Case A #2（官方当季票价）与 #5（过期活动）依赖 F2.2/F6.1，**顺延到 M2 验收**；M1 阶段这两条按"字段为空 + 标待核实 + 不编造数字"即为通过。
- **不依赖 LLM 额度**：本里程碑主体（统一对象、门禁规则集、预算口径、决策表渲染）均为纯函数 + 夹具驱动，可在无模型额度时完整开发与测试；只有回炉与端到端验收需要 LLM。

### M2 正确性与可信门面（P0/P1）——让差异"看得见"
- F2.2/F2.3 官方事实层（种子 YAML 方案）与冲突仲裁
- F3.1 时间窗模型完整化 + F3.3 动线优化（F3.2 已在 M1）
- F4.3/F4.5 交通口径与预算约束求解
- Epic 6 时效治理
- Epic 7 决策表/徽章/说明书/Plan B/避坑收敛
- 第 7 章报告信息架构落地（MD + HTML）
- **第 9 章 Case A 全集转绿（含 #2 #5）**
- **出口标准**：报告每个决策可解释，官方事实可溯源，时间/预算自洽。

### M3 性能与解耦（P1/P2）——让过程"足够快"
- **已完成（勿重做）**：F8.1 结构化取数、F8.2 资源拦截与条件等待、F8.3 多标签并发 + 全局限速（见 §1.4 实测数据）
- F8.6 要点提取结果缓存（**优先做：最大的模型成本杠杆，与§8.5 成本预算直接相关**）
- F8.5 决策/采集解耦与调参快速重排
- F8.4 视频级增量缓存（含 `cid` 字段与知识库迁移）
- 分阶段耗时埋点与性能对比（搜索/采集/证据/规划/回炉/渲染分别计时）
- **出口标准**：Demo ≤ 60 秒；调参重排零采集且 ≤1 分钟；同一关键词二次运行的要点提取 LLM 调用数为 0；采集单景点不回退（≤120 秒）且合规节奏不变。

### M4 开源冲刺（P0 for Star）——让项目"被看见、被运行、被传播"
- F9.1 离线 Demo + **合成样例数据**（结构与 M1/M2 的高质量成品一致，但店名/评论/数字均虚构，详见 F9.1 数据合规）
- F9.2 README 首屏（动图/截图/Pages 在线样例）
- F9.3/F9.4 工程化与社区基建、跨平台 CI
- F9.5 安全扫描与首个 Release（含清理已误入库的工具缓存与二进制产物）
- **出口标准**：干净环境 60 秒看成品；陌生访客 30 秒看懂价值；三平台可跑；仓库零敏感泄漏。

---

## 11. 简历与开源叙事（本期核心收益，同步建设）

### 11.1 一句话项目定位（用于仓库 About / 简历标题）

> 一个基于短视频真实评论的**可解释 AI 行程决策引擎**：自动完成 UGC 采集、观点抽取、置信度交叉验证、营销号识别、带质量门禁的闭环行程规划，每个推荐可溯源、每个落选有依据。

### 11.2 技术标签（GitHub Topics / 简历技能映射）

`llm-agent` `rag` `ugc-mining` `information-extraction` `evidence-fusion` `fastapi` `mcp` `text-analytics` `sentiment-analysis` `python` `browser-automation` `knowledge-base`

### 11.3 简历可直接使用的描述模板（按 STAR 精炼，量化处用真实数据替换）

- 独立设计并实现多源 UGC → 证据融合 → 闭环规划的 AI Agent 管线：浏览器采集 + 评论立场抽取 + 置信度分级与交叉验证 + 营销号识别，将非结构化评论转化为可溯源的结构化旅行知识。
- 设计**规划-质检-回炉（Plan-Check-Repair）决策闭环**与可解释输出：用确定性质量门禁对 LLM 行程做独立校验与自动修复，解决大模型生成"看似合理实则漏点/超支/时段冲突"的问题，黄金用例选点准确率与预算自洽率达到 100%。
- 工程化：FastAPI + MCP + CLI 三端入口、异步任务与取消、本地知识库缓存与增量更新、LLM 输出防御性规范化、**1400 行 / 66 项离线测试用例与 CI**；采集层以"接口结构化取数 + 条件等待 + 全局限速多标签并发"把单景点采集从约 300 秒压到 **66 秒（4.5 倍）**，评论取数反而从约 100 条提升到 **180 条**且 100% 带精确时间戳。
- 开源工程：提供零配置 Demo、在线样例、数据源适配器扩展机制与完整文档，GitHub 收获 X+ Star（上线后据实填写）。

### 11.4 冲 Star 的传播要点（供作者运营，不属于开发需求）

- 主受众是做 Agent/RAG/舆情分析的**开发者**（学习工程范式）+ 被网红攻略坑过的**年轻旅行者**（情绪共鸣）。
- 传播钩子："专门拆台网红景点的 AI""100 条真实评论照出谁在营销""会自我检查并重排的 AI 行程规划师"。
- 渠道：开发者社区（技术过程/架构拆解）+ 内容社区（城市避坑榜单，把项目产出变成持续内容引流）。

---

## 12. 术语表

| 术语 | 含义 |
|---|---|
| 基线（Baseline） | 模型常识/联网检索给出的候选与兜底知识，负责正确性下限 |
| 官方事实 | 景区/官方票务/政务等权威来源的票价、开闭园、预约、活动时效 |
| UGC | 抖音用户评论/视频文案等真实用户生成内容 |
| 证据强度 | 交叉验证后的强/中/弱分级，结合来源数与立场一致性 |
| 营销号占比 | 命中营销话术的来源占比，过高则对其热度/推荐降权 |
| 质量门禁 | 独立于生成模型的确定性规则裁判，决定通过/回炉/显式妥协 |
| 回炉（Repair） | 携带具体问题清单让规划器重做，且只采纳严格变好的版本 |
| 决策说明书 | 向用户解释为何入选/落选、预算如何取舍、备选与待核实项 |
| 黄金用例 | 用真实城市脱敏夹具构成的回归基线，任何改动后必须通过 |

---

## 13. 现状模块地图（供实现者定位职责，勿据此机械改代码，以实际代码为准）

- `crawler/`：浏览器采集与登录态；性能优化（Epic 8）主要落在此层。
- `pipeline/candidates.py`：候选圈定与交叉验证；Epic 1/2 主要落点。
- `pipeline/extract.py / verify.py / heat.py`：立场提取、置信度、热度/情感/避坑；证据层主体。
- `pipeline/planner.py`：档案、规划、预算、渲染；Epic 3/4/5/7 主要落点。
- `service/trip.py / research.py / heatrefresh.py`：任务编排与数据流串联；统一对象与"采集/决策解耦"主要落点。
- `core/`：LLM 封装、知识库、合规、去标识化、地理、语音等基础能力。
- `api_server.py / mcp_server.py / web/ / templates/`：三端入口与呈现层。
- `tests_offline.py`：离线测试与黄金用例归宿。

---

## 14. 关键权衡记录（避免实现者反复摇摆）

1. **为什么不直接让 LLM 自我检查、而要独立规则门禁？** 同一模型既当运动员又当裁判不可靠；确定性规则可测试、有下限、能写进简历成为技术亮点。
2. **为什么补官方事实层而不是全信评论？** 评论负责"体验与避坑"，但价格/开闭园/预约是硬事实，必须权威校准，否则会出现漏算门票、过期活动这类硬伤。**但官方事实不追求自动抓取**（无统一 API、逐站爬不可维护、LLM 联网搜索的数字仍是模型输出），而是走"仓库内种子 YAML + 高德 POI + 置空待核实"三层降级（见 F2.2），宁可缺也不编。
3. **为什么并发只到 2~3 标签且要全局限速？** 本项目以"合规可信"为卖点，提速不能以封号/法律风险为代价；稳定可复现比极限速度更重要。实现上采用**预约式令牌桶**（类级共享时间轴）而非简单互斥锁：后到线程排队尾，因此并发度提高不会缩短请求间隔（已有离线测试断言）。
4. **为什么要做 Demo 模式？** 开源转化漏斗中"跑不起来"是最大流失；让访客零成本看到高质量成品，是 Star 与简历作品口碑的前提。
5. **为什么选点决策表连淘汰点也展示？** "有证据地说不"比"一味推荐"更能建立信任，也是与通用攻略最直观的差异。

---

（完。实现时按第 10 章里程碑推进，每个 Feature 对照其 AC 自检，每个里程碑用第 9 章黄金用例回归。）
